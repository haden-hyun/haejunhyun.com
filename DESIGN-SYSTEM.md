# 🎺 DESIGN-SYSTEM.md — haejunhyun.com

> 이 저장소의 디자인 방향을 설명하는 단일 문서. "지금 뭘 해야 하는가"는
> 이 문서만 보면 된다. "왜 이렇게 됐는가"는 `CHANGELOG.md` 참고.
>
> [2026-08-05] 선행 문서 5종(`handoff.md`, `REDESIGN-GUIDE.md`,
> `design-handoff.md`, `MIGRATION-NOTES.md`, 그리고 fork로 딸려온
> `CODE_OF_CONDUCT.md`)을 삭제했다. 앞의 넷이 갖고 있던 판단 근거는
> 플러그인 소스 주석 안으로 옮겨 자체 완결되게 만들었으므로, 코드를 읽을 때
> 별도 문서를 함께 펴야 할 이유는 없다.

---

## 1. 브랜드 컨셉 — "Chill & Blue Note"

거창한 기술 포트폴리오가 아니라 **무심하고 쿨한 아날로그 지식 아카이브**를
지향한다. 데이터 엔지니어링·GIS·코드부터 기타 연주·필사까지, 각 잡힌
정규 앨범이 아닌 **B-side 트랙 / 데모 테이프**의 정서.

레퍼런스는 세 개이며, 서로 다른 매체이므로 **분리해서 구현한다**.

| 레퍼런스 | 매체 | 감각 | 상태 |
|---|---|---|---|
| Blue Note Records | 인쇄물(LP 커버) | 타이포그래피·여백·제한 팔레트 | ✅ 적용 완료 (§2, §3) |
| Blue Note Bar | 공간(클럽) | 다크모드 = 어둠+단일 광원 | ◐ 배경만 적용 (§3.1) · 광원 기각 (§5.1) |
| Blue Giant | 영상(재즈 영화) | 트리거된 순간의 모션 | ◐ 다크 배경색 적용 (§3.1) · 모션 미착수 (§5.2) |

원본 색 추출에 쓴 커버는 `palette-sample-img/`에 보관한다
(`true-blue.jpeg` / `blue-note.jpeg` / `blue-midnight.jpeg` / `blue-giant.png`).

톤앤매너: Eyebrow `blue notes. 🎷` / Headline "어쩌다 보니 모인 관심사의
파편들" / CTA `Play Notes` — 절제된 위트, 전면 재즈 은유는 지양.

---

## 2. 타이포그래피 — 적용 완료

원칙: **무드는 표면(브랜드 존)에, 가독성은 본문(콘텐츠 존)에.**

| 슬롯 | 서체 | 적용 범위 |
|---|---|---|
| title | Anton (400) | 로고 `HAEJUN RECORDS`만 |
| header | Hahmlet (400/700/900) | 모든 헤딩(h1~h6), 콜아웃 제목 |
| body | Noto Sans KR | 본문, 사이드바, Explorer |
| code | JetBrains Mono | 코드, 날짜·분량 메타 |

- 설정 위치: `quartz.config.yaml`의 `theme.typography` + `fonts` 플러그인
  옵션(두 값은 항상 같게 유지 — 후자가 최종 렌더 폰트 결정).
- 로고(Anton)는 헤딩(Hahmlet)과 의도적으로 분리 — 브랜드 목소리와 콘텐츠
  목소리를 나눈다. 로고는 본문 어디에도 인라인으로 등장하지 않으므로
  같은 줄에서 두 서체가 부딪히는 지점이 없다.
- 기각된 대안: Gothic A1 — 본문(Noto, 산세리프)과 헤딩이 같은 계열이 되어
  서체 대비로 만들어지던 위계가 사라짐. IBM Plex Sans KR(본문) — 재실측
  비용 대비 무드 기여가 작아 기각.
- h1만 자간 -0.02em, h2 이하는 0 (Hahmlet은 자소 간격이 넉넉해 조이면
  한글이 붙는다).

---

## 3. 컬러 — "True Midnight" 적용 완료 (2026-08-05)

레퍼런스 커버 4장(`palette-sample-img/`)에서 실제 픽셀 분포를 추출해
재구성한 팔레트. **라이트=크림 인쇄지(True Blue 커버), 다크=잉크
네이비(Blue Giant)** 로 §1의 매체 분리를 색으로 구현한다.

### 3.1 토큰

| 역할 | 라이트 | 대비 | 다크 | 대비 |
|---|---|---|---|---|
| bg (`light`) | `#f7f5ef` | — | `#0e1420` | — |
| border (`lightgray`) | `#e4dfd4` | — | `#1e2836` | — |
| meta (`gray`) | `#676f7c` | 4.65 AA | `#7c8899` | 5.12 AA |
| body (`darkgray`) | `#323e4c` | 9.99 AAA | `#bcc9d9` | 10.96 AAA |
| heading (`dark`) | `#141d28` | 15.58 AAA | `#e9eff7` | 15.93 AAA |
| accent (`secondary`) | `#1c5f95` | 6.17 AA | `#6fb2e0` | 8.00 AAA |
| accent hover (`tertiary`) | `#17527f` | 7.55 AAA | `#9dcdec` | 10.86 AAA |

- accent의 색상(H207)은 True Blue 커버(H208)와 Midnight Blue 커버(H202)의
  중간값 — 두 레퍼런스가 실제로 교차하는 지점이다.
- `tertiary`는 **hover 시 진해지는** 방향이다. 라이트 배경에서 밝아지는
  기존 방식(`#6a9ab8`)으로는 AA를 만족시킬 수 없다(2.90:1).

### 3.2 웜 액센트 2종 — 색이 곧 의미

Blue Note의 원래 정체성이 블랙+시안+브릭 3색이었던 것처럼 웜 계열을 둘
둔다. 단, **"예쁜 색 두 개"로 쓰면 화면이 산만해지므로 역할을 고정한다.**

| 토큰 | 라이트 | 다크 | 역할 | 성격 |
|---|---|---|---|---|
| `--accent-2` (Chitlins) | `#b03a7d` 5.14 | `#dd7ab0` 6.57 | **행동** — Hero CTA | 드물게 등장, 시선을 끈다 |
| `--accent-code` (Brick) | `#b8442a` 4.94 | `#e87256` 6.12 | **기술 리터럴** — 인라인 코드 | 자주 섞이므로 튀면 안 된다 |

- 두 색은 색상환에서 **45° 떨어져** 있다(H326 vs H11). 이 간격이 좁아지면
  역할 구분이 무너지므로, 값을 조정할 땐 대비뿐 아니라 **상호 색상차도
  함께** 확인할 것.
- 부수 효과: 기존엔 CTA와 본문 링크가 같은 블루라 "누르는 것"과 "이동하는
  것"이 구분되지 않았다. 이제 핑크는 화면에서 CTA 한 곳에만 나타난다.
- 출처: Chitlins = `blue-midnight.jpeg` 마젠타(`#cc84b4`),
  Brick = `blue-note.jpeg`의 "uncompromising expression" 브릭(`#cc543c`).

### 3.3 단일 소스 규칙 — 실제로 지켜지게 고침

색의 단일 소스는 `quartz.config.yaml`의 `theme.colors`이고, `custom.scss`는
별칭만 정의한다. **다만 이 원칙은 그동안 실제로는 깨져 있었다** —
`custom.scss`에 `rgba(61,107,142,·)` 리터럴이 15곳 하드코딩돼 있어서
config에서 accent를 바꿔도 태그·테이블·콜아웃·블록쿼트는 옛 색으로 남았다.

- `--accent-rgb`(삼원색만 보관) 토큰을 신설해 15곳 전부 치환했다.
  `rgba()` 안에서 CSS 변수는 값 치환으로 동작한다.
- ⚠️ `secondary`를 바꿀 땐 `--accent-rgb`도 **반드시 같이** 바꿀 것.
  두 값이 같은 색을 가리킨다는 보장이 문법 차원엔 없다.
- 인라인 코드의 `#8b4513`(브라운)도 제거했다 — 팔레트 어디에도 근거가 없는
  고립된 하드코딩이었다.

### 3.4 크림 배경을 고른 이유 — 정확히

"크림이 눈에 편하다"는 **인쇄물 인체공학에서 온 통념이고 자발광
디스플레이에선 근거가 약하다.** 가독성은 대비비와 극성이 지배하며 바탕
색조는 부차적이다. 실측 효과는 광량 95.5% → 91.3% 수준.

채택 근거는 **"더 잘 읽혀서"가 아니라 "컨셉이 맞고 가독성 손해가 없어서"**다.
텍스처·노이즈 없이 색만으로 인쇄지 질감을 낸다(§5 "넘지 말아야 할 선" 준수).

알려진 비용 — **둘 다 감수하기로 결정, 향후 보완**:

1. 흰 배경 이미지가 바탕보다 9.5% 밝게 뜬다(기존 4.7%의 2배). 본문 이미지
   30장 대부분이 matplotlib·다이어그램 출력물로 추정된다.
2. 코드블록(`catppuccin-latte` `#eff1f5`)이 한랭 회색이라 크림 위에서
   색온도차 +14가 된다(기존 +8). `#f8f5ed`(−11) 대신 `#f7f5ef`(−8)로
   절제해 격차를 줄여 뒀다.

### 3.5 토픽 컬러 — 보류

10슬롯 체계(40° 균등 hue 배치), 장식용(`--c-*`)과 라벨 텍스트용
(`--c-*-text`, AA 보정)을 분리해서 쓴다. **이번 작업에서는 손대지 않았다.**

- 미해결: Chitlins가 `--c-prog`(`#d6267e`)와 색상차 **3°** — 태그와 CTA가
  나란히 놓이면 같은 색으로 읽힌다. Brick도 `--c-gis`(`#db2e0c`)와 **1°**이나,
  채워진 버튼 vs 텍스트 라벨로 **형태가 구분 채널을 하나 더 제공**한다.
- 미해결: 배경이 `#fafaf8 → #f7f5ef`로 바뀌었으므로 `--c-*-text` 10슬롯의
  AA 값을 재계산해야 한다(변화폭이 작아 대부분 통과 예상).
- 한 칸만 옮기면 40° 균등 배치가 깨지므로, 손댈 땐 10슬롯을 통째로 볼 것.

---

## 4. 남은 개선 항목

핸드오프 원본의 P0~P2 중 미착수 항목만 남긴다.

- [ ] **팔레트 실물 렌더 확인** — §3 적용분은 SCSS 컴파일과 대비 계산으로만
      검증했다. 전체 빌드가 `.quartz/plugins` 부트스트랩 순환
      (`npm run install-plugins` → `quartz.ts` → `.quartz/plugins`)에 막혀
      브라우저 확인을 못 했다. 팔레트 변경과 무관한 환경 문제.
- [ ] 인라인 코드 Brick(S77) 채도 재검토 — 본문에 자주 나오는 요소라 실제
      글에서 강하게 느껴질 수 있다. 대비 유지한 완화안: `#a9503c`(S65) /
      `#9d5749`(S54), 셋 다 4.94~4.95로 동일.
- [ ] 코드블록 syntax theme를 웜 계열로 교체 (§3.4 비용 2 해소)
- [ ] 콜아웃 타입별 색상 복구 확인 (라이트·다크 실물 확인 필요)
- [ ] TOC sticky + 대비 보강
- [ ] 헤딩 스케일 재조정 (h1 32 / h2 24 / h3 19.2 / h4 17px)
- [ ] 본문 16 → 17px
- [ ] Explorer sticky
- [ ] frontmatter `description` 수동 보강 (요약 6건 미보유 노트)
- [ ] 모바일 실기기 검증

---

## 5. 무드 확장 — 예정 (미착수)

### 5.0 레퍼런스 컬러 비교

| | 현재 사이트 | Blue Note Bar(도쿄) | Blue Giant(애니메이션) |
|---|---|---|---|
| 채도 | 낮음(절제) | 중간(네온↔브릭 대비) | 매우 높음(전기적) |
| 명도 범위 | 좁음(가독성 우선) | 중간(dim 조명) | 극단적(잉크블랙→일렉트릭블루) |
| 목적 | 장문 독서 | 공간의 분위기 | 감정이 폭발하는 순간 |
| 대표색 | `#1c5f95` True Midnight | 네이비+베이지, 블루 네온, 브릭/가죽 웜톤 | `#020b2d`→`#1d6eea`→`#dbeafe` |

두 레퍼런스 모두 "몰입 환경"용 색이라 장문 읽기용 색이 아니다. 그대로
들이면 채도 피로와 §3 AA 대비 붕괴가 생긴다 → **본문·텍스트 색은
그대로 유지, 다크모드 배경과 액센트에만 선택적으로 반영**.

### 5.1 Blue Note Bar — 다크모드 (부분 적용)

**라이트=커버, 다크=클럽** 프레이밍은 §3.1에서 배경색으로 구현됐다
(`#1a1e24` → `#0e1420`, Blue Giant 잉크 네이비 쪽으로 한 단계 깊게).
"차가운 블루 vs 따뜻한 브릭"의 온도 대비도 §3.2의 웜 액센트 2종으로 들어왔다.

- ~~Hero 뒤 웜 radial-gradient 한 점~~ → **기각 (2026-08-05).** 시안까지
  만들어 확인했으나 사용자 판단으로 제외. 무드는 배경색과 액센트만으로
  낸다. 되살릴 경우 텍스트 영역 바깥에만 둘 것 — 본문 뒤에 깔면 위치마다
  AA 대비가 달라져 §3 계산이 무효화된다.
- 미착수: 카드 hover 시 미세 발광
  (`box-shadow` 대신 `0 0 24px rgba(111,178,224,0.12)`)
- 다크모드 기본값 여부는 미결 — 라이트 기본 유지 + 다크를 "숨겨진 두
  번째 얼굴"로 두는 안을 권장.

### 5.2 Blue Giant — 트리거된 모션

상시 애니메이션이 아니라 **hover/scroll에 반응하는 순간**만, 일렉트릭
블루(`#1d6eea` 계열)를 포인트로.

- `reading-progress` 바를 `--secondary → 일렉트릭 블루` 그라디언트로
- Play Notes CTA hover 시 0.2s 발광
- Featured 카드 hover 시 좌측 accent bar가 위→아래로 그려지는 트랜지션

필수: `prefers-reduced-motion: reduce` 대응, SPA 재바인딩(`nav` 이벤트).

### 넘지 말아야 할 선

배경 텍스처·노이즈(본문 뒤 금지), 커서 트레일/파티클, 사운드, "본문
폰트를 더 재즈스럽게"(본문은 무드의 영역이 아님).
