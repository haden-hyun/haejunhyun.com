# DESIGN-SYSTEM.md

> 디자인의 현재 상태를 기록하는 단일 문서. **"지금 뭘 해야 하나" = 이 문서.**
> "왜 이렇게 됐나" = `CHANGELOG.md`.
> 색·서체를 건드리기 전에 **§6(고칠 때 규칙)을 먼저 볼 것.**
>
> `haejun-redesign-palette.html`은 빌드 없이 열리는 정적 목업이다. 값을 바꾸면
> 목업도 같이 고칠 것 — 손으로 쓴 마크업이라 자동으로 안 따라온다.

---

## 0. 상태 요약

| 영역 | 상태 | 위치 |
|---|---|---|
| 타이포그래피 | 적용 | §2 |
| 컬러 토큰 | 적용 | §3 |
| 콜아웃·인라인코드·표 | 적용 | §4.1~4.3 |
| 코드 하이라이팅 | 적용 | §4.5 |
| 한글 폰트 폴백 | 적용 | §4.4 |
| 토픽 컬러 폐지 | 적용 | §4.6 |
| 웜 액센트 3축 | 적용 | §3.2 |
| 하이라이트·강조 재질화 | 적용 | §4.7 |
| 노트 3패널 개편 | 적용 | §4.8 |
| 이미지 레이아웃 | 적용 | §4.9 |
| 상단 내비 + Featured 큐레이션 | 적용 | §4.10 |
| 배포 | 정상 | §5 |

---

## 1. 컨셉 — "Chill & Blue Note"

- 기술 포트폴리오 아님. **무심하고 쿨한 아날로그 지식 아카이브.**
- 정규 앨범이 아니라 **B-side / 데모 테이프**의 정서.
- 톤: eyebrow `blue notes. 🎷` / CTA `Play Notes` — 절제된 위트, 전면 재즈 은유 금지.

| 레퍼런스 | 가져온 것 | 상태 |
|---|---|---|
| Blue Note Records | 타이포·여백·제한 팔레트 | 적용 |
| Blue Note Bar | 다크 배경 | ◐ 배경만 (광원 기각) |
| Blue Giant | 다크 잉크 네이비 | ◐ 색만 (모션 미착수) |

원본 커버: `palette-sample-img/` 4장 — 팔레트는 여기서 픽셀 추출.

---

## 2. 타이포그래피

**원칙: 무드는 표면에, 가독성은 본문에.**

| 슬롯 | 서체 | 범위 |
|---|---|---|
| title | Anton 400 | 로고만 |
| header | Hahmlet 400/700/900 | 헤딩, 콜아웃 제목 |
| body | Noto Sans KR | 본문, 사이드바 |
| code | JetBrains Mono | 코드, 메타 (한글은 Noto Sans KR로 폴백 → §4.4) |

- 설정 2곳(`theme.typography` + `fonts` 플러그인 옵션)을 **항상 같게** 유지. 후자가 최종 결정.
- 로고(Anton)를 헤딩(Hahmlet)과 분리 = 브랜드 목소리와 콘텐츠 목소리 분리.
- h1만 자간 −0.02em, h2 이하 0 (Hahmlet은 조이면 한글 자소가 붙는다).
- 기각: Gothic A1(본문과 같은 산세리프 → 위계 소실), IBM Plex Sans KR(무드 기여 작음).

---

## 3. 컬러 — "True Midnight"

**라이트 = 크림 인쇄지 / 다크 = 잉크 네이비.**
accent H207 = True Blue 커버(H208)와 Midnight Blue 커버(H202)의 중간값.

### 3.1 기본 토큰 (`quartz.config.yaml` → `theme.colors`)

| 역할 | 라이트 | 다크 |
|---|---|---|
| bg `light` | `#f7f5ef` | `#0e1420` |
| border `lightgray` | `#e4dfd4` | `#1e2836` |
| meta `gray` | `#676f7c` 4.65 | `#7c8899` 5.12 |
| body `darkgray` | `#323e4c` 9.99 | `#bcc9d9` 10.96 |
| heading `dark` | `#141d28` 15.58 | `#e9eff7` 15.93 |
| accent `secondary` | `#1c5f95` 6.17 | `#6fb2e0` 8.00 |
| hover `tertiary` | `#17527f` 7.55 | `#9dcdec` 10.86 |

- **hover는 진해지는 방향.** 라이트에서 밝아지면 대비를 못 맞춘다(옛 `#6a9ab8` = 2.90).

### 3.2 세 축 (`custom.scss`)

| 축 | 토큰 | 라이트 | 다크 | 적용처 |
|---|---|---|---|---|
| **공간** 여기서 저기로 | `--secondary` | `#1c5f95` | `#6fb2e0` | 링크, 백링크, breadcrumb, CATALOG 현재 항목 |
| **행위** 누르는 것 | `--accent-action` (Brick) | `#b8442a` 4.94 | `#e87256` 6.12 | Play Notes CTA |
| **시간** 읽는 중 | `--accent-state` (Chitlins) | `#b03a7d` 5.14 | `#dd7ab0` 6.57 | 읽기 진행바, **TOC 활성 마커** |

- 각 색이 답하는 질문이 달라 역할 침범이 없다. 인라인 코드는 어디에도 속하지 않으므로 색이 아니라 재질로 구분한다(§4.2). 하이라이트도 같은 이유로 재질이다(§4.7).
- **TOC 활성 마커가 시간 축인 이유** — "지금 내가 읽고 있는 위치"라 읽기 진행바와 같은 질문에 답한다. 블루로 두면 링크(공간 축)와 신호가 겹친다. 상단 진행바와 우측 TOC가 같은 색을 쓰면 "현재 위치"가 두 곳에서 일관되게 나온다.
- **초판은 Chitlins=CTA, Brick=인라인코드였다.** 채도·빈도가 역전돼 있어 교체했다 — Brick(S77)이 문단마다 나오는 코드에, Chitlins(S67)가 페이지당 1회인 CTA에 있었다. 강도는 중요도에 비례하고 빈도에 반비례해야 한다.
- 두 웜 색은 색상환에서 45° 떨어져 있다(H11 vs H326). 값 조정 시 대비뿐 아니라 상호 색상차도 확인할 것.

### 3.3 크림 배경의 근거

- **"크림이 눈에 편하다"는 통념이다.** 자발광 디스플레이에선 근거가 약하고, 가독성은 대비·극성이 지배한다. 실측 효과는 광량 95.5%→91.3%.
- 채택 이유는 **"더 잘 읽혀서"가 아니라 "컨셉이 맞고 가독성 손해가 없어서"**.
- 감수한 비용: ① 흰 배경 이미지가 9.5% 밝게 뜬다 ② 코드블록이 한랭해 보였다 → **§4.4에서 해소**.

---

## 4. 컴포넌트

### 4.1 콜아웃 — 6그룹 시맨틱

| 그룹 | 타입 | 라이트 | 다크 |
|---|---|---|---|
| 정보 | note, abstract, info, todo, example | `--cal-info` = accent | 〃 |
| 긍정 | tip, success | `#187755` 4.60 | `#4cbf87` |
| 주의 | warning, question | `#8a600f` 4.69 | `#d9a441` |
| 위험 | danger, failure, bug | `#b8324a` 4.83 | `#e8695c` |
| 출처 | quote | `#6b6560` 4.82 | `#9a938c` |
| 참고 | **reference** *(커스텀)* | `#6b3fa0` 6.77 | `#b39ae0` 7.56 |

- 사용량(실측): abstract 87 · quote 70 · note 51 · warning 48 · tip 19 · 나머지 1~3.
- **토픽 컬러는 폐지하면서 콜아웃은 색을 쓰는 이유**: 토픽은 *명명*, 콜아웃은 *가치*. 색은 명명엔 약하고 가치엔 강하다(빨강=위험은 보편 관습). 콜아웃은 하나씩 등장하지 10개가 경쟁하지 않는다.
- 보정 2건: 위험은 크림슨(H349) — 통상 `#c0392b`(H6)는 Brick(H11)과 **5°**라 구분 불가. 주의는 명도를 낮춤 — 흔한 `#a86a12`는 **4.07로 AA 미달**.
- 주의: **대비는 페이지 배경이 아니라 콜아웃 틴트 배경 기준으로 잰다**(CLAUDE.md §7).
  옛 `#1a7f5a`는 페이지 배경에선 4.56이지만 자기 틴트 위에선 **4.28로 AA 미달**이었다.
- **형태는 Obsidian 규격을 유지한다** — 아이콘 + 둥근 모서리 + 좌측 3px 바.
  주의: 아이콘을 걷어내고 위아래 괘선 + 대문자 제목으로 바꾸는 "인쇄 조판" 안은
  2026-08-06에 시도했다가 되돌렸다(형태가 낯설어져 읽기가 나빠졌다). 다시 시도하지 말 것.
  우리가 정의하는 건 **색(5그룹)이지 형태가 아니다.**
- 구현: `--color`만 그룹 단위로 재정의. `callouts.scss`가 이미 제목에 쓰고 있어 박스까지 따라온다.
- **`[!reference]`는 Obsidian 표준에 없는 커스텀 타입**이다. ofm 파서가 미지의 타입도 `data-callout`에 그대로 실어 보내므로 CSS만으로 추가된다.
  - `example`을 쓰지 않은 이유: example은 *"이 개념을 보여주는 사례"*, reference는 *"이건 어디서 왔나"* — 의도가 다르다.
  - **2026-08-06 원고 마이그레이션**: `[!quote]` 70개 중 69개가 제목이 "Reference"/"참고사이트"이고
    내용이 출처 링크 목록이었다(진짜 인용문 0). 설계가 원고를 앞질렀던 것 —
    vault의 해당 67건을 `[!reference]`로 치환해 실제 용법과 맞췄다. 남은 `quote` 2건은 데모용.

### 4.2 인라인 코드 — 색이 아니라 재질

- 세 축 어디에도 안 속하므로 **색을 주지 않는다.** 대신 칩을 강화.
- 안 보이던 원인은 글자색이 아니라 **보더 대비 1.11**이었다 → 1.47로.

| | 라이트 | 다크 |
|---|---|---|
| `--code-chip-bg` | `#eae4d6` | `#1b2432` |
| `--code-chip-border` | `#c6bda8` | `#2c3a4d` |
| `--code-ink` | `#443e36` 8.33 | `#c9c0b4` 8.69 |

### 4.3 표

- **헤더 확정** — accent 8% 배경 + 2px accent 하단 보더 + `--dark` bold.
- 얼룩말 무늬 **3.5%**, hover **10%**.
- hover가 얼룩보다 확실히 진해야 한다(옛 4%면 얼룩진 행에서 안 보임).
- 주의: 얼룩과 행 보더를 둘 다 진하게 쓰면 중복 — 얼룩은 옅게 유지.


### 4.4 한글 폰트 폴백 — 수식·코드

한글 글리프가 없는 서체가 쓰이는 자리에서 브라우저 기본 폰트(Windows = 굴림/바탕)로
떨어져 본문과 전혀 다른 얼굴이 되던 문제. **빌드 실측: 코드 294곳 · 수식 59곳.**

| 위치 | 원인 | 처방 |
|---|---|---|
| 코드블록·인라인 | JetBrains Mono에 한글 없음 | `code` 스택에 `"Noto Sans KR"` 삽입 |
| 수식 `\text{}` | KaTeX 폰트에 한글 없음 | `.katex .hangul_fallback → --bodyFont` |

- **`font-family` 폴백은 글리프 단위로 동작한다** — 라틴은 JetBrains Mono/KaTeX,
  한글만 Noto Sans KR로 간다. Noto Sans KR은 이미 body용으로 로드돼 있어 요청 추가 0.
- KaTeX는 한글에 **`hangul_fallback` 클래스를 자동으로 붙여준다.** 그것만 잡으면 된다.
- `\text{}` 안의 라틴도 본문 서체로 통일했다 — 그 명령은 "수식 안의 산문"용이고,
  본문이 산세리프라 KaTeX_Main(세리프)이 문장 중간에서 이질적이었다.
  주의: 수학 기호·변수(`.mathnormal` 등)는 건드리지 않는다. 그건 KaTeX 폰트가 맞다.
- 주의: **한계**: Noto Sans KR은 모노스페이스가 아니라 한글·라틴 폭이 정확히 2:1이 아니다.
  한글 섞인 줄끼리 세로 정렬이 어긋날 수 있다(주석 시작 위치는 앞의 라틴이 결정하므로
  대개 문제되지 않음). 완전 해결은 Nanum Gothic Coding 같은 한글 모노스페이스가 필요한데,
  `theme.typography`가 슬롯당 폰트 하나만 받아 별도 로딩 경로가 있어야 한다.

### 4.5 코드 하이라이팅

**Shiki 테마 색을 CSS로 전면 교체.** `quartz/styles/syntax.scss` = 코드 색의 단일 소스.

| 그룹 | 토큰 | 라이트 | 다크 |
|---|---|---|---|
| comment *(italic)* | comment | `#6e685e` 4.63 | `#7e8794` 4.90 |
| keyword | keyword·important | `#9e3a16` 5.74 | `#e8946a` 7.29 |
| punct | punctuation·operator | `#5f5850` 5.88 | `#98a2b0` 6.68 |
| value | value·number | `#1a6152` 6.13 | `#6fc4aa` 8.34 |
| alert | regexp·error | `#9c1327` 6.95 | `#f08498` 6.95 |
| function | function·property·tag | `#7c2f5e` 7.27 | `#dda2c8` 8.28 |
| string | string | `#14456b` 8.42 | `#9ecbee` 10.05 |
| ink | **기본값** · 식별자 | `#26221c` 13.28 | `#e9eff7` 14.92 |

스프레드 8.65 / 10.02 · 최저 4.63 / 4.90 · **AA 미달 0**.

**설계 두 축**

1. **명도가 1차 신호, 색상이 2차 신호.** 유채색이 한 명도대에 몰리면 색상만으로
   구별해야 해서 스캔이 느려진다. 주석 가장 옅게 → 구두점 → 키워드·숫자·함수 →
   문자열 → **식별자 가장 진하게**(실제로 읽는 내용).
2. **채도가 아니라 명도로 강조.** 웜 크림지(S6) 위에서 밝고 채도 높은 색은
   "형광펜"처럼 뜬다. Gruvbox Light가 웜 배경에서 호평받는 이유는 채도가 아니라
   **어둡기(V≈60)** — 화면 색이 아니라 **인쇄 잉크**로 읽힌다.
   그래서 유채색을 눌렀다: function V69→49 · value V58→38 · keyword V72→62.

**브랜드 결합** — 유채색 4종이 전부 사이트 색상에 붙는다. 고아 색상 없음.

| 토큰 | 색상 | 대응 |
|---|---|---|
| string | H206 | `--secondary` 공간·블루 (1°) |
| keyword | H16 | `--accent-action` 행위·브릭 (5°) |
| function | H323 | `--accent-state` 시간·Chitlins (3°) |
| value | H167 | 딥 틸 — 유일한 신규 잉크 |

**주의: 함정 3가지**

- **기성 라이트 테마의 색 값을 차용하지 말 것.** 우리 배경이 그 테마들의 배경보다
  어두워 대비가 무너진다. 우리 배경 위 AA 미달 실측 —
  Solarized Light **9/9** · Rosé Pine Dawn **6/8** · Gruvbox Light **4/9** ·
  GitHub Light **3/7**. 차용 대상은 색이 아니라 **구조**다.
- **기본색은 `--syn-ink`. `--shiki-light/dark`로 폴백하지 말 것.**
  Shiki는 식별자에 `data-token-type`을 붙이지 않는다(렌더 실측 7,598 토큰 중
  1,058개가 타입 없음 = 변수명 전부. `normal` 타입은 37개뿐인 죽은 타입).
  폴백을 Shiki에 맡기면 **가장 많이 읽는 변수명이 통제 밖으로 나간다.**
- **대비는 코드블럭 배경 기준으로 잰다.** 페이지 배경(`#f7f5ef`)으로 재면
  블럭이 더 어두워 어긋난다 (`#1a7f5a`는 페이지 4.56 / 블럭 4.17 미달).

`keepBackground: false` → Shiki 배경 제거, `pre` 배경은 `--code-bg`가 담당
(라이트 `#efebe0` / 다크 `#141b28`).

### 4.6 토픽 — 색이 아니라 타이포그래피


- **8~10개를 색으로 구분하는 설계를 접는다.**
  1. 범주형 색 구분 실용 한계는 6~8개. 작은 라벨·3px 바에선 더 낮다.
  2. 40° 균등 10슬롯은 색각이상(남성 ~8%)에서 적록이 붕괴 — GIS(적)/Finance(녹) 구분 불가.
  3. **"블루 + 웜 2색"이 정체성인데 10색 무지개를 얹으면 무지개가 브랜드를 이긴다.**
  4. 색은 그룹핑엔 강하나 명명엔 약하다. 토픽은 명명 문제.
- 대체: **라벨 타이포가 식별자.** 색은 "현재 보는 토픽"에만.
- **비율 바는 남긴다** — 색코딩이 아니라 분포(Programming 38 vs Tools 4)라는 실제 정보.
- Explorer는 이미 `a.active { color: var(--secondary) }` — 그대로 두면 된다.
- 제거한 것: 토큰 20개(`--c-*` 10 + `--c-*-text` 10), 매핑 3곳
  (`TopicGrid.tsx`, `TopicsContent.tsx`, `FeaturedNotes.tsx`), 3px 컬러바
  (`.topic-card-bar`, `.topics-page-bar`).
- 라벨은 이미 1.15rem/670으로 무게를 지고 있다(이모지 제거 때 키운 값).
- Featured 카테고리 라벨도 중립(`--text-3`)으로 통일 — 대문자·자간·굵기가 위계를 만든다.

### 4.7 하이라이트 · 강조 — 색이 아니라 재질/무게

- **`==하이라이트==`도 세 축에 넣지 않는다.** "명명"도 "가치"도 아닌 "여기 주목"이라
  네 번째 색을 만들면 축 체계가 무너진다. 인라인 코드와 같은 해법 — 색상이 아니라
  **명도로만** 구분하는 웜 뉴트럴 밴드.
- **채움이 아니라 밑줄 밴드**(`box-shadow: inset 0 -0.45em 0`). 채움으로 두면
  `--code-chip-bg`와 값이 가까워 혼동된다. 칩=박스, 하이라이트=밑줄로 형태를 가른다.
- 폐기: 형광 노랑 `#fde68a88` / 올리브 `#b3aa0288` — 팔레트 어디에도 근거가 없었다.
- 주의: **밴드는 "밴드 위 본문" 대비만으로 검증하면 안 된다.** 원래 값(`#e5dcc4`/`#2a3444`)은
  본문 대비 7.99/7.40 AAA로 통과했지만 **배경 대비가 1.25/1.47이라 밴드 자체가 거의
  보이지 않았다**(2026-08-06 사용자 지적 → 실측 확인). 두 대비를 함께 본다.

| `--textHighlight` | 라이트 | 다크 |
|---|---|---|
| 밴드 | `#ccb684` | `#36435a` |
| **배경 대비**(밴드가 보이는가) | 1.82 | 1.85 |
| 밴드 위 본문 대비 | 5.49 AA | 5.93 AA |

- 주의: **`<mark>`를 함께 잡을 것.** ofm은 `==...==`에만 `.text-highlight`를 붙여서,
  원고가 직접 쓴 `<mark>`가 브라우저 기본 **형광 노랑(`#ffff00`)**으로 새고 있었다.
  폐기했다던 형광 노랑이 다른 경로로 살아 있던 셈이다.

- **`strong`은 `--dark`를 쓴다.** 헤딩이 Hahmlet, 본문이 Noto Sans KR이라 **서체가
  이미 둘을 가르기 때문**이다. 헤딩까지 본문 서체이던 시절엔 같은 색·같은 흐름·같은
  서체가 겹쳐 굵은 문장이 소제목처럼 읽혔는데, 그 조건이 사라졌다.
- **`em`은 기울이지 않는다.** Noto Sans KR엔 진짜 이탤릭이 없어 브라우저가 합성
  오블리크를 만들고, 한글엔 이탤릭 관습 자체가 없다. 구분은 자간(`0.02em`)이 맡는다.
- 주의: **`em`을 `--cal-quote`로 되돌리지 말 것.** 그 값(6.08)은 **본문 p(10.96)보다
  연해서** 강조인데 오히려 물러나 보였다(2026-08-06 실측). `--darkgray`로 올려
  역전을 없앴다. 인용/출처와 잉크를 공유하던 "다른 목소리" 해석은 폐기 —
  원고의 실제 `em` 용례 38회는 대부분 이미지 캡션이라 위치가 이미 그 역할을 한다.
- 주의: 두 규칙은 **레이어 밖 + `article` 스코프**여야 한다. 레이어 안에 두면
  base의 `p > strong { color: var(--dark) }`가 명시도로 이기고, 전역 요소
  선택자를 레이어 밖에 두면 플러그인 UI가 깨진다.

### 4.8 노트 상세 3패널

**좌측 = SPECS → CATALOG** (2026-08-07). 좌측이 "이 음반은 무엇인가"를 위에서 아래로
답하고(사양 → 수록곡), 우측은 본문 탐색(TOC·백링크·그래프)만 맡는다. SPECS는 원래
우측 최상단이었는데, 그러면 우측이 "메타 + 탐색" 두 성격을 섞어 들고 있었다.

**좌측 CATALOG** — 전체 파일 트리(explorer) 폐기. 현재 노트의 형제 노트만 보여준다.

- 길이가 **형제 수에만** 비례하므로 노트가 늘어도 한 화면을 넘지 않는다.
  (구 트리는 폴더 32 · 파일 링크 89개를 전부 DOM에 렌더했다.)
- 펼침 상태가 존재하지 않아 explorer의 localStorage 누적 문제가 구조적으로 사라진다.
- 라벨은 **CATALOG** — "Explorer"는 IDE 용어라 은유 밖이고, 접힘 폭에서 잘렸다.
- 3층 분업이 선명해진다: 상단 GlobalNav = 레이블 카탈로그 / 좌측 = 지금 이 판의
  트랙 리스트 / 우측 = 이 트랙 안의 구조. 구 트리는 `/topics`와 기능이 겹쳤다.
- 구현: `plugins/note-catalog`. 형제 목록은 `allFiles` 런타임 집계이며,
  주의: `frontmatter.title`이 아니라 **슬러그 패턴**으로 자동 생성 페이지를 거른다.

**우측 3패널 높이 배분** — 순서 TOC → Backlinks → Graph.

| 패널 | flex | 비고 |
|---|---|---|
| TOC | `1 1 0` + `min-height` | 남는 공간 전부, 내부 스크롤 |
| Backlinks | `0 0 auto`, `max-height: 9rem` | 내부 스크롤 |
| Graph | `0 0 auto`, **기본 접힘** | 클릭 시 확장 |

- 이전엔 셋 다 `flex: 0 1 auto`라 TOC가 자연 높이를 다 먹고, 861px 사이드바에서
  백링크가 **52px(한 줄)** 이었다. base가 백링크 목록에 건
  `max-height: calc(100% - 2rem)`도 부모 높이가 auto라 해석되지 않고 무시됐다.
- 주의: `min-height: 0`을 빼지 말 것 — flex 자식 기본값이 `min-height: auto`라
  내용보다 작아지지 않는다. 이게 없으면 flex 값을 뭘 주든 줄지 않는다.
- 주의: TOC를 감싼 `.desktop-only`는 `display: contents`라 flex에 투명하다.
  래퍼가 아니라 `.toc`/`.backlinks`/`.graph`를 직접 타겟해야 한다.

**헤딩 — Hahmlet 복귀 + h2 액센트**

| | 서체 | 색 |
|---|---|---|
| h1 / `.article-title` | Hahmlet 800 | `--dark` `#141d28` |
| h2 | Hahmlet 700 + 하단 괘선 | `--secondary` `#1c5f95` |
| h3 | Hahmlet 700 | `--dark` `#141d28` |

**`--dark`는 검정이 아니다** — `#141d28`은 H213 S50으로 True Midnight(H207)과 6° 차이,
블루 계열의 가장 깊은 잉크다(§3의 "잉크 네이비"). 헤딩 3단계는 전부 블루 계열이다.

주의: **h3를 `--darkgray`로 되돌리지 말 것** — 그 값(`#323e4c`)은 본문 `p`와 **완전히 같은
색**이라 h3가 색으로는 본문과 구분되지 않았다. 블루로도 갈 수 없다: 라이트 모드에 블루가
2개뿐인데 더 깊은 Deeper Cut(7.55)이 h2의 True Midnight(6.17)보다 **강해서 위계가 뒤집힌다.**

h2에 링크와 같은 블루를 써도 안전한 근거: **링크는 항상 문단 안(인라인), 헤딩은 항상
블록 단독**이라 같은 흐름에서 경쟁하지 않는다. 색이 아니라 위치가 둘을 가른다.
표 헤더(`thead`)만 본문 서체로 남긴다 — 헤딩이 아니라 데이터 라벨이다.

**패널 라벨 5종은 한 규칙으로 통일한다** — SPECS · CATALOG · Table of Contents ·
Backlinks · Graph View 전부 헤딩 기본값(Hahmlet 700 / 1rem / `--darkgray` / margin 0).
넷이 서로 다른 조판이면 같은 종류의 물건으로 읽히지 않는다.

주의: 모노 대문자 + 패널별 축 색으로 나누는 안은 2026-08-06에 시도했다가 되돌렸다 —
라벨이 콘텐츠보다 시끄러워지고 "Table of Contents"가 대문자로 뭉갠다. 다시 하지 말 것.

**패널 항목도 한 규칙으로 통일한다** (2026-08-07) — CATALOG 노트 · TOC 절 ·
Backlinks 노트 전부 **Hahmlet / 0.8rem / `--gray`**, hover `--secondary`.
셋 다 "다른 곳으로 가는 링크"라 같은 물건이다. 예전엔 CATALOG 0.82 / TOC 0.78 /
Backlinks **미지정(본문 크기 상속)** 이라 백링크만 유독 커 보였다.

**색은 목록이 아니라 "지금 여기" 한 줄에만 쓴다.** CATALOG의 현재 노트와 TOC의 현재
절만 `--secondary`, 나머지는 전부 `--gray`. 둘이 같은 색이어야 "지금 여기"가 사이드바
전체에서 한 부호로 읽힌다.

주의: 항목 전체를 `--secondary`로 채우는 안은 2026-08-07에 시도했다 되돌렸다 —
사이드바가 통째로 파래져 정작 현재 위치가 묻힌다.
주의: TOC 활성을 `--accent-state`(chitlins)로 되돌리지 말 것 — 그러면 CATALOG 현재 노트와
TOC 현재 절이 서로 다른 색이 되어 같은 질문에 두 부호로 답한다. 읽기 진행바는 위치가
아니라 **진행률**이므로 `--accent-state`를 그대로 쓴다.
주의: 웜 액센트는 둘 다 못 쓴다 — Chitlins는 읽기 진행바와, Brick은 CTA(페이지당 1회)와
신호가 겹친다.

주의: **`.note-catalog-link.active` 색은 `custom.scss`에도 반드시 있어야 한다.**
플러그인의 `.active` 규칙은 `@layer quartz-base` **안**이고 항목 기본 규칙은 레이어
밖이라, 명시도(0,3,0 > 0,2,0)와 무관하게 `--gray`가 이겨 **현재 노트까지 회색이 된다.**
비-레이어끼리 다시 붙여야 명시도가 작동한다. 플러그인 쪽엔 **형태만** 남긴다.

**SPECS 블록** — 제목 바로 아래에 뭉쳐 있던 날짜·읽기시간·태그를 **좌측 최상단**
음반 사양 표기로 옮겼다(`plugins/note-specs`). 제목에서 본문으로 가는 길이 비워진다.
데스크톱 전용이라 모바일은 기존 `content-meta`/`tag-list`가 `display: mobile-only`로 맡는다.
주의: 읽기시간은 `reading-time` 패키지를 **이식**했다 — 두 경로가 다른 숫자를 내면 안 되고,
패키지를 의존성으로 넣으면 CJS라 ESM 번들에서 컴포넌트 로드가 통째로 죽는다.

**Breadcrumb** — 카탈로그 번호 스트립. JetBrains Mono 0.7rem, 자간 0.08em,
조상 `--text-3` / 현재 페이지는 링크 해제 + `--text-2`.
로고(Anton) → breadcrumb(Mono) → 제목(Noto)으로 세 층의 목소리가 갈린다.

**TOC 대비** — 주의: `opacity: 1`을 되돌리지 말 것. base가 비활성 항목에 거는
`opacity: .35`는 `--gray` 위에 곱해져 실효 대비를 **1.6:1**로 떨어뜨린다(AA 위반).
활성/비활성 구분은 투명도가 아니라 색과 굵기가 맡는다.

### 4.9 이미지 레이아웃 — 캡션은 이미지 **아래**

`plugins/image-layouts`가 Obsidian의 ` ```image-layout ` 펜스를 그리드·메이슨리·
캐러셀로 렌더한다. 색은 전부 테마 변수에서 오고, 예외가 없다.

- **캡션을 사진 위에 얹지 않는다.** 원 플러그인은 오버레이(`hover`/`always`)를
  제공하지만, 사진 위 텍스트는 배경 명도를 통제할 수 없어 AA를 보장하려면 테마 밖
  고정색 스크림이 필요해진다 — "색의 단일 소스" 규칙이 깨진다. 아래로 내리면
  `--gray`(4.65 / 5.12)로 이미 검증된 대비를 그대로 쓴다. **되살리지 말 것.**
- `overlay: never`만 "캡션 숨김"으로 살렸다 — Obsidian에서도 안 보이므로 결과가 같다.
- 영역 이름은 프리셋·custom 그리드 모두 `image-{n}`. 이름을 공유해 CSS 경로가 하나다.
- 주의: `.il-viewport`에 `scroll-behavior: smooth`를 넣지 말 것. `scroll-snap: mandatory`
  스크롤러에서 애니메이션이 스냅에 먹혀 **프로그램적 스크롤이 통째로 무시된다**(실측).

### 4.10 상단 내비 · Featured 큐레이션

**상단 내비는 `Home · Topics · Archive · Notes`.** 넓은 것에서 좁은 것 순이고,
전용 페이지가 없는 Notes를 끝에 둔다.

Notes는 두 가지로만 동작한다.

| 현재 페이지 | 렌더 |
|---|---|
| 실제 노트 | `<span aria-current="page">` — 링크 아님 |
| 그 외 전부 | 가장 최근 노트로 가는 `<a>` |

태그·폴더 인덱스에서는 네 항목이 모두 비활성이다. 그 페이지들이 네 목적지 중
어디에도 속하지 않는다는 사실을 그대로 표시한다.

**Featured는 `quartz.config.yaml`의 `slugs` 배열이 유일한 지정 수단이다.**
쓴 순서가 표시 순서고 첫 항목이 대형 카드. 4개를 못 채우면 토픽 라운드로빈으로
자동 채운다. `frontmatter.featured`는 지원하지 않는다 — 지정하는 곳이 두 군데면
어디서 정해졌는지를 매번 추적해야 한다.

주의: 미해결·중복·초과 슬러그는 빌드 로그에 경고로 찍힌다. 조용히 무시되지 않는다.

---

---

## 5. 빌드와 배포

`content/`가 Obsidian vault를 가리키는 심볼릭 링크라 원격 빌드가 불가능하다.
그래서 빌드 결과물 `public/`을 커밋해 Cloudflare가 그대로 서빙한다.

```bash
npx quartz plugin install   # .quartz/ 생성 (gitignore된 생성 디렉터리)
npx quartz build            # public/ 갱신 — 이걸 빼면 라이브에 반영되지 않는다
```

- 플러그인 소스를 고쳤으면 해당 플러그인에서 `npm run build`(tsup)를 먼저 돌린다.
  `dist/`가 커밋 대상이다.
- 렌더 결과는 `cd public && python3 -m http.server 8899`로 실측한다.
- 주의: `--serve`는 에셋을 해시 없는 이름으로 내보낸다. 그대로 커밋하면 캐시
  무효화가 깨지므로, 서버를 끈 뒤 `npx quartz build`로 다시 구울 것.
- 주의: `--serve`가 도는 중에 `npx quartz build`를 겹쳐 돌리면 출력 디렉터리가
  충돌해 `public/`이 비워질 수 있다. 한 번에 하나만 돌린다.

---

## 6. 고칠 때 규칙

1. **색의 단일 소스는 `quartz.config.yaml` → `theme.colors`.** `custom.scss`는 별칭만.
2. **`secondary`를 바꾸면 `--accent-rgb`도 반드시 같이.** 두 값이 같은 색을 가리킨다는 보장이 문법에 없다. (예전에 `rgba(61,107,142,·)` 리터럴 15곳이 하드코딩돼 원칙이 깨져 있었다.)
3. **`blockquote:not(.callout)`의 `:not`을 절대 빼지 말 것.** 콜아웃 루트도 `<blockquote>`라, 빼면 `custom.scss`가 non-layered로 항상 이겨 **모든 콜아웃이 파랑으로 납작해진다.**
4. **전역 요소 선택자(h1~h6, p 등)는 `@layer quartz-base` 안에.** 밖에 두면 명시도와 무관하게 플러그인 스타일을 깨뜨린다.
5. **웜 액센트 조정 시 대비 + 상호 색상차 둘 다 확인.** 45° 아래로 좁아지면 역할 구분이 무너진다. 단 채도가 크게 다르면 색상차는 의미가 적다.
6. **테이블 hover는 얼룩보다 진하게.** `nth-child`와 명시도가 같아 **순서 의존** — hover가 뒤에 와야 한다.
7. **플러그인은 `dist/`가 커밋 대상.** 소스 수정 후 `npm run build` 필수.
8. **코드 토큰 색은 `syntax.scss`에서 바꾼다.** `quartz.config.yaml`의 `theme`는
   오버라이드가 없는 토큰의 폴백일 뿐이다.
9. **`--code-bg`를 바꾸면 `syntax.scss`의 6개 토큰을 재검증할 것.** 대비가
   그 값 기준으로 계산돼 있다.
10. **`syntax.scss`의 기본색을 `--shiki-light/dark`로 되돌리지 말 것.**
    Shiki가 식별자에 토큰 타입을 안 붙여서, 폴백을 Shiki에 맡기면 변수명이
    통제 밖으로 나간다(§4.5).
11. **AA 기준은 4.5, 예외 없음.** 텍스트로 렌더되는 모든 것(본문·링크·라벨·
    메타·콜아웃 제목·코드 토큰)에 적용. 순수 장식(보더·얼룩·구분선)은 요구 없음.
    큰 글씨(≥24px, ≥18.7px bold)와 상태 표시 UI 경계는 3.0.
12. **배경 위에 얹는 면(하이라이트 밴드·얼룩)은 대비를 두 번 잰다** — 그 위 텍스트
    대비와 **페이지 배경과의 대비**. 후자를 빼먹으면 AAA를 받고도 안 보인다(§4.7).
13. **강조 계열은 본문 p보다 진해야 한다.** 강조인데 본문보다 연하면 물러나 보인다.
    `em`이 정확히 그 상태였다(6.08 < 10.96).
14. **본문에 흩뿌려지는 것에 축 색(brick·Chitlins)을 주지 말 것.** 강도는 중요도에
    비례하고 **빈도에 반비례**한다 — 볼드는 원고에서 813회 나온다(아래 참조).

---

## 7. 백로그

- [ ] **frontmatter 날짜 키 불일치** — `created-modified-date`는 `created`/`modified`/
      `published`만 읽는데 노트 91개 중 90개가 `updated:`를 쓴다. 수정일이 파일
      mtime으로 떨어져 Recent Notes 정렬·표시 날짜·"최신 노트" 링크가 흔들린다.
- [ ] TOC sticky · Explorer sticky
- [ ] 헤딩 스케일 재조정 (h1 32 / h2 24 / h3 19.2 / h4 17px)
- [ ] 본문 16 → 17px
- [ ] frontmatter `description` 보강 (요약 미보유 6건)
- [ ] 모바일 실기기 검증

---

## 8. 무드 확장 — 미착수

- **Blue Note Bar**: 카드 hover 미세 발광 `0 0 24px rgba(111,178,224,.12)`.
  ~~Hero 뒤 radial-gradient 광원~~ → **기각**(시안까지 만들었으나 제외).
- **Blue Giant**: 진행바 그라디언트, CTA hover 발광, Featured 카드 accent bar 드로잉.
  필수: `prefers-reduced-motion`, SPA 재바인딩(`nav` 이벤트).
- **넘지 말 선**: 본문 뒤 텍스처·노이즈, 커서 트레일/파티클, 사운드, 본문 서체를 무드용으로 바꾸기.
