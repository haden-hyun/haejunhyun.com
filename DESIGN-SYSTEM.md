# DESIGN-SYSTEM.md

> 디자인의 현재 상태를 기록하는 **단일 문서**. 색·서체를 건드리기 전에 §5를 먼저 볼 것.

---

## 1. 컨셉

**Blue Note / 빈티지 음반 레이블.** 기술 포트폴리오가 아니라 아날로그 지식 아카이브.
정규 앨범이 아니라 B-side / 데모 테이프의 정서 — 절제된 위트, 전면 재즈 은유 금지.

---

## 2. 타이포그래피

원칙: **무드는 표면에, 가독성은 본문에.**

| 슬롯   | 서체                | 범위                                         |
| ------ | ------------------- | -------------------------------------------- |
| title  | Anton 400           | 로고만                                       |
| header | Hahmlet 400/700/900 | 헤딩, 콜아웃 제목                            |
| body   | Noto Sans KR        | 본문, 사이드바                               |
| code   | JetBrains Mono      | 코드, 메타 (한글은 Noto Sans KR 폴백 → §4.4) |

- 로고(Anton)와 헤딩(Hahmlet)을 나눠 브랜드 목소리와 콘텐츠 목소리를 분리한다.
- h1만 자간 −0.02em. Hahmlet은 조이면 한글 자소가 붙는다.
- 주의: `theme.typography`와 `fonts` 플러그인 옵션 **두 곳을 항상 같게** (후자가 최종 결정).

---

## 3. 컬러 — "True Midnight"

라이트 = 크림 인쇄지 / 다크 = 잉크 네이비. accent H207.

### 3.1 기본 토큰 (`quartz.config.yaml` → `theme.colors`)

| 역할               | 라이트          | 다크            |
| ------------------ | --------------- | --------------- |
| bg `light`         | `#f7f5ef`       | `#0e1420`       |
| border `lightgray` | `#e4dfd4`       | `#1e2836`       |
| meta `gray`        | `#676f7c` 4.65  | `#7c8899` 5.12  |
| body `darkgray`    | `#323e4c` 9.99  | `#bcc9d9` 10.96 |
| heading `dark`     | `#141d28` 15.58 | `#e9eff7` 15.93 |
| accent `secondary` | `#1c5f95` 6.17  | `#6fb2e0` 8.00  |
| hover `tertiary`   | `#17527f` 7.55  | `#9dcdec` 10.86 |

- **hover는 진해지는 방향.** 라이트에서 밝아지면 대비를 못 맞춘다.
- `--dark`는 검정이 아니라 H213의 가장 깊은 잉크다. 헤딩 3단계가 전부 블루 계열.
- 주의: 한 테마에서 쓸 수 있는 블루는 2개뿐이다.

### 3.2 세 축 (`custom.scss`)

색이 답하는 질문이 서로 달라 역할 침범이 없다.

| 축                     | 토큰              | 라이트         | 다크           | 적용처                                          |
| ---------------------- | ----------------- | -------------- | -------------- | ----------------------------------------------- |
| **공간** 여기서 저기로 | `--secondary`     | `#1c5f95`      | `#6fb2e0`      | 링크, 백링크, breadcrumb, CATALOG 현재 항목, h2 |
| **행위** 누르는 것     | `--accent-action` | `#b8442a` 4.94 | `#e87256` 6.12 | Play Notes CTA                                  |
| **시간** 읽는 중       | `--accent-state`  | `#b03a7d` 5.14 | `#dd7ab0` 6.57 | 읽기 진행바, TOC 활성                           |

- 세 축에 속하지 않는 신호(인라인 코드·하이라이트·bold)는 **색이 아니라 재질/무게**로 구분한다. 네 번째 색을 만들면 축 체계가 무너진다.
- 강도는 중요도에 비례하고 **빈도에 반비례**한다 — 본문에 흩뿌려지는 것에 축 색을 주지 않는다.

### 3.3 무대(stage) — 컷아웃이 서는 면 (`custom.scss` §0)

세 축과 별개의 **면**이다. 인물 컷아웃과 포스터 배경이 여기 선다 — 홈 히어로와 푸터가 공유한다.

| 토큰               | 라이트          | 다크                     | 비고                             |
| ------------------ | --------------- | ------------------------ | -------------------------------- |
| `--stage`          | `#141d28`       | `#1e2836`                | 각각 `--dark` / `--lightgray`    |
| `--stage-fade`     | `0.5`           | `0`                      | 판 상단 48px에 페이지색 그라디언트 |
| `--stage-edge`     | transparent     | `rgba(233,239,247,.09)`  |                                  |
| `--stage-text`     | `#e9eff7` 14.69 | 〃 12.86                 | AAA                              |
| `--stage-text-2`   | `#c8d4e2` 11.30 | 〃 9.89                  | AAA                              |
| `--stage-text-3`   | `#8fa0b5` 6.36  | 〃 5.57                  | AA                               |
| `--stage-accent`   | `#6fb2e0` 7.38  | 〃 6.46                  | = `darkMode.secondary`           |
| `--stage-accent-2` | `#9dcdec` 10.01 | 〃 8.77                  | = `darkMode.tertiary`            |

- **판은 두 모드 모두 밤이다. 라이트에서 밝은 판은 성립하지 않는다** — 컷아웃의 흰 옷이 형태를
  유지하려면 판이 최소 3:1 어두워야 하는데(`#8d9aa8`도 2.80으로 미달), 그 밝기면 판 자체가
  크림 대비 2.6이라 이미 "밝은 판"이 아니다. **밝게 하면서 인물을 살리는 값은 없다 — 다시 시도하지 말 것.**
- 라이트에서 판 경계가 튀는 문제는 색이 아니라 **포스터 배경 + 상단 페이드**로 눅인다.
- 다크에선 페이지가 이미 밤이라 판을 한 단계 띄우고 테두리로 경계를 만든다 — 카드가 `--surface` + border로 서는 방식과 같다.
- 배경 이미지 위에는 잉크 베일을 덮어 글자 대비를 `--stage` 단색과 동등하게 유지한다(푸터 0.86 / 히어로 0.64).
- 베일 위에서는 `--stage-accent`가 3.70으로 소형 텍스트 AA에 미달한다 → `--stage-accent-2`를 쓴다(5.02).
- 주의: 페이드 높이(48px)가 그 아래 텍스트 블록의 상단 패딩(56px)을 넘으면 글자 대비가 깎인다.
- 주의: 컷아웃은 판 바닥에 닿아야 한다. 띄우면 어디에 서 있는지 읽히지 않는다.

---

## 4. 컴포넌트

### 4.1 콜아웃 — 6그룹 시맨틱

**역할**: 문단의 *가치*를 색으로 부호화한다. 토픽(명명)과 달리 색이 맞는 신호다.

| 그룹 | 타입                                | 라이트                | 다크           |
| ---- | ----------------------------------- | --------------------- | -------------- |
| 정보 | note, abstract, info, todo, example | `--cal-info` = accent | 〃             |
| 긍정 | tip, success                        | `#187755` 4.60        | `#4cbf87`      |
| 주의 | warning, question                   | `#8a600f` 4.69        | `#d9a441`      |
| 위험 | danger, failure, bug                | `#b8324a` 4.83        | `#e8695c`      |
| 출처 | quote                               | `#6b6560` 4.82        | `#9a938c`      |
| 참고 | `reference` _(커스텀)_              | `#6b3fa0` 6.77        | `#b39ae0` 7.56 |

- 구현은 `--color` 재정의 하나. `callouts.scss`가 제목에 쓰고 있어 박스까지 따라온다.
- **형태는 Obsidian 규격 유지** — 아이콘·둥근 모서리·좌측 바. 우리가 정의하는 건 색이지 형태가 아니다.
- 주의: 대비는 페이지 배경이 아니라 **콜아웃 틴트 배경 기준**으로 잰다.

### 4.2 인라인 코드 · 하이라이트 · 강조 — 재질과 무게

**역할**: 세 축 밖의 신호를 색 없이 구분한다.

| 대상             | 수단                                                                    |
| ---------------- | ----------------------------------------------------------------------- |
| 인라인 코드      | 칩(배경 + 보더). 보더 대비가 보이게 하는 핵심                           |
| `==하이라이트==` | 밑줄 밴드(`box-shadow: inset`). 칩=박스 / 하이라이트=밑줄로 형태를 가름 |
| `strong`         | `--dark`. 서체가 이미 헤딩과 본문을 가르므로 색만으로 충분              |
| `em`             | 기울이지 않고 자간 `0.02em`. 한글엔 이탤릭 관습이 없다                  |

- 주의: `<mark>`도 함께 잡는다. ofm은 `==...==`에만 클래스를 붙여 원고의 `<mark>`가 브라우저 기본 형광 노랑으로 샌다.
- 주의: 강조 계열은 본문 `p`보다 진해야 한다. 연하면 강조인데 물러나 보인다.

### 4.3 표

**역할**: 데이터 라벨과 값의 위계.

- 헤더 = accent 8% 배경 + 2px accent 하단 보더 + `--dark` bold. 표 헤더만 본문 서체.
- 얼룩말 3.5% / hover 10%. 주의: hover가 얼룩보다 진해야 하고, `nth-child`와 명시도가 같아 **순서 의존**이다.

### 4.4 한글 폰트 폴백 — 수식·코드

**역할**: 한글 글리프가 없는 서체 자리에서 브라우저 기본 폰트로 떨어지는 것을 막는다.

| 위치            | 처방                                   |
| --------------- | -------------------------------------- |
| 코드블록·인라인 | `code` 스택에 `"Noto Sans KR"` 삽입    |
| 수식 `\text{}`  | `.katex .hangul_fallback → --bodyFont` |

- `font-family` 폴백은 **글리프 단위**로 동작한다 — 라틴은 원래 서체, 한글만 넘어간다.
- 주의: Noto Sans KR은 모노스페이스가 아니라 한글 섞인 줄의 세로 정렬이 어긋날 수 있다.

### 4.5 코드 하이라이팅

**역할**: Shiki 테마 색을 전면 교체한다. `quartz/styles/syntax.scss`가 **코드 색의 단일 소스**.

| 그룹                    | 라이트          | 다크            |
| ----------------------- | --------------- | --------------- |
| comment _(italic)_      | `#6e685e` 4.63  | `#7e8794` 4.90  |
| keyword                 | `#9e3a16` 5.74  | `#e8946a` 7.29  |
| punct                   | `#5f5850` 5.88  | `#98a2b0` 6.68  |
| value                   | `#1a6152` 6.13  | `#6fc4aa` 8.34  |
| alert                   | `#9c1327` 6.95  | `#f08498` 6.95  |
| function                | `#7c2f5e` 7.27  | `#dda2c8` 8.28  |
| string                  | `#14456b` 8.42  | `#9ecbee` 10.05 |
| ink **(기본값·식별자)** | `#26221c` 13.28 | `#e9eff7` 14.92 |

설계 두 축: **명도가 1차 신호, 색상이 2차**(주석 가장 옅게 → 식별자 가장 진하게) / **채도가 아니라 명도로 강조**(크림지 위에서 밝고 채도 높은 색은 형광펜처럼 뜬다).

- 유채색 4종은 전부 사이트 색상에 붙는다(string=공간, keyword=행위, function=시간, value=딥 틸).
- 주의: 기성 라이트 테마의 색 값을 차용하지 말 것. 우리 배경이 더 어두워 대비가 무너진다.
- 주의: **기본색을 `--shiki-light/dark`로 되돌리지 말 것.** Shiki가 식별자에 토큰 타입을 안 붙여서, 폴백을 맡기면 변수명이 통제 밖으로 나간다.
- 주의: 대비는 **코드블럭 배경**(`--code-bg`) 기준으로 잰다.

### 4.6 토픽 — 색이 아니라 타이포그래피

**역할**: 8~10개 토픽을 라벨 타이포로 식별한다. **토픽 컬러는 폐지됐다 — 되살리지 말 것.**

- 색은 그룹핑엔 강하나 명명엔 약한데, 토픽은 명명 문제다.
- 10색 무지개는 "블루 + 웜 2색"이라는 정체성을 이긴다. 색각이상에서 적록도 붕괴한다.
- 색은 **현재 보는 토픽**에만 쓴다. 비율 바는 색코딩이 아니라 분포 정보라 남긴다.

### 4.7 노트 상세 3패널

**역할**: 좌측 = 이 음반은 무엇인가(SPECS → CATALOG) / 본문 / 우측 = 본문 탐색(TOC → Backlinks → Graph).

| 패널                             | 핵심                                                          |
| -------------------------------- | ------------------------------------------------------------- |
| SPECS (`plugins/note-specs`)     | 날짜·읽기시간·태그를 좌측 최상단 음반 사양으로. 데스크톱 전용 |
| CATALOG (`plugins/note-catalog`) | 전체 파일 트리 폐기, **형제 노트만**. 길이가 형제 수에만 비례 |
| TOC                              | `flex: 1 1 0` + `min-height: 0` — 남는 공간 전부, 내부 스크롤 |
| Backlinks                        | `0 0 auto`, `max-height: 9rem`                                |
| Graph                            | `0 0 auto`, 기본 접힘                                         |

- **패널 라벨 5종은 한 규칙**(Hahmlet 700 / 1rem / `--darkgray`). 패널별로 나누면 라벨이 콘텐츠보다 시끄러워진다.
- **패널 항목도 한 규칙**(Hahmlet / 0.8rem / `--gray`). 셋 다 "다른 곳으로 가는 링크"라 같은 물건이다.
- **색은 "지금 여기" 한 줄에만.** CATALOG 현재 노트와 TOC 현재 절만 `--secondary`.
- 주의: `min-height: 0`을 빼면 flex 값을 뭘 주든 줄지 않는다.
- 주의: `.note-catalog-link.active` 색은 `custom.scss`에도 있어야 한다. 플러그인 규칙은 레이어 안이라 명시도와 무관하게 진다.
- 주의: TOC 비활성에 `opacity: .35`를 되돌리지 말 것 — 실효 대비 1.6:1로 AA 위반.

### 4.8 이미지 레이아웃 — 캡션은 이미지 **아래**

**역할**: `plugins/image-layouts`가 ` ```image-layout ` 펜스를 그리드·메이슨리·캐러셀로 렌더한다.

- 사진 위 텍스트는 배경 명도를 통제할 수 없어 AA를 보장하려면 테마 밖 고정색이 필요해진다 → **오버레이 캡션은 되살리지 말 것.**
- 주의: `.il-viewport`에 `scroll-behavior: smooth` 금지 — `scroll-snap: mandatory`에서 프로그램적 스크롤이 통째로 무시된다.

### 4.9 상단 내비 · Featured

**역할**: 사이트 전체의 목적지 4개(`Home · Topics · Archive · Notes`)와 홈 큐레이션.

- Notes는 실제 노트 위에서 `<span aria-current="page">`, 그 밖에서는 가장 최근 노트로 가는 링크.
- **Featured는 `quartz.config.yaml`의 `slugs` 배열이 유일한 지정 수단.** 쓴 순서 = 표시 순서, 첫 항목이 대형 카드, 부족분은 토픽 라운드로빈으로 자동 채움.
- 주의: 미해결·중복·초과 슬러그는 빌드 로그 경고로 찍힌다. 조용히 무시되지 않는다.

### 4.10 페이지 폭 · 좌우 여백

**역할**: `.page`가 좌우 여백의 **단일 소스**다 (`custom.scss` §10.4).

```scss
width: min(100% - var(--page-gutter) * 2, $pageMaxWidth); // 거터 2rem(모바일 1rem), 상한 1500px
```

- 거터 `2rem`은 **사이드바 내부 패딩과 같은 값**이다. 새 상수를 만들지 않는다.
- 뷰포트 1564px가 경계 — 아래는 거터 32px 고정, 위로는 남는 폭이 그대로 여백. 본문은 최대 850px.
- topbar는 폭을 `.page`에서 받고 배경·구분선만 `::before`로 번진다. 로고와 본문 시작점이 자동으로 맞는다.
- 주의: 폭 계산에 `vw`를 쓰지 말 것 — 세로 스크롤바 폭을 포함해 좌우가 어긋난다.
- 주의: `::before`의 `-100vmax` 블리드는 **`body`의 `overflow-x: clip`**이 자른다. `html`에 걸면 viewport로 전파돼 무효다.

---

## 5. 고칠 때 규칙

**공통 규칙 10개는 `CLAUDE.md`의 "절대 어기면 안 되는 것"이 원본이다.**
여기엔 그 목록에 없는 색·대비 전용 규칙만 남긴다.

1. **전역 요소 선택자(h1~h6, p 등)는 `@layer quartz-base` 안에.** 밖에 두면 명시도와 무관하게 플러그인 스타일을 깨뜨린다.
2. **`--code-bg`를 바꾸면 `syntax.scss`의 토큰 대비를 전부 재검증할 것.**
3. **배경 위에 얹는 면(하이라이트 밴드·얼룩·베일)은 대비를 두 번 잰다** — 그 위 텍스트 대비와 **페이지 배경과의 대비**. 후자를 빼먹으면 AAA를 받고도 안 보인다.
4. **웜 액센트 조정 시 대비 + 상호 색상차 둘 다 확인.** 45° 아래로 좁아지면 역할 구분이 무너진다.

---

## 6. 백로그

- [ ] **frontmatter 날짜 키 불일치** — `created-modified-date`는 `created`/`modified`/`published`만 읽는데 노트 91개 중 90개가 `updated:`를 쓴다. 수정일이 파일 mtime으로 떨어져 Recent Notes 정렬·표시 날짜가 흔들린다.
- [ ] **사이드바 유연폭** — 320px 고정이라 좁은 데스크톱에서 여백이 본문에서만 나온다. `minmax(260px, .5fr)`로 바꾸면 1440에서 본문 726 → 846px. 260px에서 패널 라벨 조판이 버티는지 미검증.
- [ ] **avatar.png 최적화** — 695KB(484×894)를 220px 높이로 렌더 중. WebP 238×440이면 40KB(−94%).
- [ ] TOC sticky · Explorer sticky
- [ ] 헤딩 스케일 재조정 (h1 32 / h2 24 / h3 19.2 / h4 17px)
- [ ] 본문 16 → 17px
- [ ] frontmatter `description` 보강 (요약 미보유 6건)
- [ ] 모바일 실기기 검증

---

## 7. 무드 확장

- **Blue Note Bar**: 카드 hover 미세 발광. Hero 뒤 radial-gradient 광원은 **기각**.
- **Blue Giant**: 진행바 그라디언트, CTA hover 발광, Featured 카드 accent bar 드로잉. 필수: `prefers-reduced-motion`, SPA 재바인딩.
- **넘지 말 선**: 본문 뒤 텍스처·노이즈, 커서 트레일/파티클, 사운드, 본문 서체를 무드용으로 바꾸기.
