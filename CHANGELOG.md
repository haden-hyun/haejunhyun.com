# CHANGELOG.md — 디자인 결정 이력

> "지금 상태"는 `DESIGN-SYSTEM.md`. 여기는 "왜 이렇게 됐는가"의 타임라인만.

## 2026-08-08 — 상단 내비를 `Home · Topics · Archive · Notes`로, Notes에 목적지를 주다

**순서가 바뀌었다** (`Home | Notes | Topics | Archive` → `Home | Topics | Archive | Notes`).
Notes가 둘째 자리에 있으면서 목적지가 없는 게 앞줄의 모순이었다. 넓은 것에서 좁은 것으로
가는 순서로 놓고, 목적지 없는 항목을 끝으로 뺐다.

**Notes의 "전용 페이지가 없다"를 페이지를 만들지 않고 해결했다.** 예전 규칙은
"Home/Topics/Archive 중 무엇도 아니면 Notes가 활성"이라는 **부정 판정**이었고, 링크는
`/archive`로 보냈다. 그래서 태그 페이지·폴더 인덱스·404까지 Notes로 뭉뚱그려졌다 —
노트가 아닌 곳에서 "노트"라고 표시하고 있었던 것이다.

지금은 두 줄이다. **노트 위에 있으면 링크가 아닌 `<span aria-current="page">`, 그 밖에서는
가장 최근 노트로 보낸다.** 노트 페이지에서 자기 자신을 가리키는 href를 쓰지 않은 건
스크롤이 튀고 히스토리만 쌓이기 때문이다. "아무 기능 없음"이 요구였으니 링크 자체를 없앴다.

부작용으로 **태그·폴더 인덱스 페이지에서는 네 항목 모두 비활성**이 됐다. 그 페이지들이
네 목적지 중 어디에도 속하지 않는다는 사실을 그대로 표시하는 쪽을 골랐다.

최신 노트 정렬은 `modified → created` 폴백 — `recent-notes-index`의 `getSortTime`과
같은 체인이다. 다르게 잡으면 **Notes가 보내는 노트와 홈 Recent Notes 맨 윗줄이 어긋난다.**

⚠️ SCSS는 손대지 않았다. `<span>`이 섞이니 hover를 `a`로 좁혀야 할 것 같았지만, 재보니
`.global-nav-link:hover`와 `.global-nav-link.active`는 명시도가 같고(0,2,0) `.active`가
뒤에 선언돼 이미 이긴다. span은 항상 `.active`를 달고 있어 hover 색이 먹지 않는다.
**증상을 예상해서 고치려 한 자리가 실제로는 이미 맞았다.**

설계 문서: `docs/superpowers/specs/2026-08-08-global-nav-notes-design.md`

## 2026-08-07 — SPECS를 좌측으로, 패널 항목을 한 규칙으로

**SPECS가 좌측 최상단으로 갔다** (SPECS → CATALOG). 우측이 "메타(SPECS) + 탐색
(TOC·백링크·그래프)" 두 성격을 섞어 들고 있었는데, 좌측이 *이 음반은 무엇인가*를
사양 → 수록곡 순으로 답하고 우측은 *본문 어디를 볼 것인가*만 맡도록 갈랐다.

**패널 항목 3종을 한 규칙으로 묶었다** — CATALOG 노트 · TOC 절 · Backlinks 노트를
Hahmlet / 0.8rem / `--secondary`로. 라벨 5종에 적용했던 논리를 항목에도 적용한 것이다.
계기는 "백링크 노트가 너무 크다"였는데, 재보니 CATALOG 0.82 / TOC 0.78 / **Backlinks는
크기 지정 자체가 없어 본문 크기를 상속**하고 있었다. 크기가 안 맞은 게 아니라 한 곳이
규칙 밖에 있었다.

**항목 색은 블루로 정하되, 목록 전체가 아니라 "지금 여기" 한 줄에만 쓴다.**
처음엔 항목 전부를 `--secondary`로 채웠다가 되돌렸다 — 사이드바가 통째로 파래지니
정작 현재 위치가 묻혔다. 색을 늘려 눈에 띄게 하려다 **색이 흔해져서 신호가 죽은** 것이다.
지금은 CATALOG 현재 노트와 TOC 현재 절만 블루, 나머지는 `--gray`다.

같은 이유로 **TOC 활성 표시를 chitlins에서 블루로 옮겼다.** 원래 근거는 "활성은 시간
축이라 읽기 진행바와 같은 질문에 답한다"였는데, 실제 화면에선 CATALOG의 현재 노트와
TOC의 현재 절이 **서로 다른 색으로 같은 질문에 답하고** 있었다. 둘 다 "지금 여기"다.
읽기 진행바는 위치가 아니라 진행률이므로 `--accent-state`를 그대로 둔다.

brick·chitlins를 항목 색으로 쓰는 안은 기각했다. 대비는 셋 다 AA를 넘지만 둘은 화면에서
각각 다른 것을 망가뜨린다 — chitlins는 읽기 진행바와, brick은 페이지당 1회여야 할 CTA와
신호가 겹친다.

⚠️ **캐스케이드 레이어 함정을 하나 더 잡았다.** 플러그인의 `.note-catalog-link.active`는
`@layer quartz-base` 안에 있고 새 항목 규칙은 레이어 밖이라, 명시도가 더 높은데도(0,3,0
> 0,2,0) `--gray`가 이겨 **현재 노트까지 회색이 될 상태**였다. 레이어 밖에 `.active`를
다시 선언해야 명시도가 작동한다. `noteCatalog.scss`의 `--gray` 지정도 함께 제거했다 —
항목 색은 `custom.scss` 한 곳에 두고 플러그인 쪽엔 형태만 남긴다.

## 2026-08-06 (4차) — 하이라이트·이탤릭 교정, 헤딩/볼드 제안은 기각

제안 셋을 원고 실측(91편: h2 370 / h3 54 / **볼드 813** / em 38 / 하이라이트 40)과
대비 계산으로 검토했다. **둘은 채택, 둘은 기각.**

**하이라이트 밴드를 진하게 했다** — `#e5dcc4`→`#ccb684`, `#2a3444`→`#36435a`.
"배경과 구분이 안 된다"는 지적이 정확했다. 원래 값은 **밴드 위 본문 대비가 7.99/7.40
AAA로 통과**했는데도 **배경 대비가 1.25/1.47**이라 밴드 자체가 안 보였다. 얹는 면은
대비를 두 번 재야 한다는 규칙(§6-12)을 여기서 얻었다 — 한쪽만 재고 AAA라 안심한 것이
원인이다. 색조는 웜 뉴트럴 그대로 두고 명도만 낮췄다(색이 아니라 재질 원칙 유지).

**`<mark>` 태그가 형광 노랑으로 새고 있었다.** ofm은 `==...==`에만 `.text-highlight`를
붙이는데 우리 규칙이 그 클래스만 잡아서, 원고가 직접 쓴 `<mark>`는 브라우저 기본
`#ffff00` + 검정으로 렌더됐다. **폐기했다고 적어둔 형광 노랑이 다른 경로로 살아 있던
셈이다.** 셀렉터에 `article mark`를 더했다.

**이탤릭을 본문 잉크로 올렸다** — `--cal-quote`(6.08)는 **본문 p(10.96)보다 연해서**
강조인데 물러나 보였다. "인용/출처와 잉크를 공유하는 다른 목소리"라는 해석은 폐기했다.
원고의 `em` 38회는 대부분 `*그림. 폭 520 지정*` 같은 이미지 캡션인데, 캡션은 위치가
이미 역할을 말하므로 색까지 물러설 이유가 없었다.

**볼드=brick / 이탤릭=Chitlins는 기각했다.** 대비는 둘 다 AA를 통과하지만(4.94·5.14),
볼드가 원고에 **813회** 나온다. 이건 2026-08-05에 "Brick이 문단마다 나오는 인라인
코드에 있다"며 되돌린 바로 그 구조다 — 강도는 중요도에 비례하고 빈도에 반비례한다.
축 색을 본문에 흩뿌리면 CTA·읽기진행바의 신호가 희석된다.

**h2/h3를 둘 다 블루로 바꾸는 안도 기각했다.** 라이트는 Deeper Cut(7.55) > True
Midnight(6.17)로 위계가 성립하지만, **다크는 Stage Light(8.00) < Rim Light(10.86)로
뒤집힌다** — 다크는 밝을수록 대비가 오르기 때문이다. 게다가 h2가 370회인 상황에서
h3까지 블루가 되면 현재 **색상 채널 자체로 갈려 있던 구분**이 명도차 1.22배만 남고,
두 블루는 링크·hover에 이미 배정돼 있어 본문에서 충돌한다.

## 2026-08-06 (3차) — image-layouts 플러그인 도입

Obsidian `obsidian-image-layouts`의 ` ```image-layout ` 문법을 Quartz가 빌드 타임에
렌더한다(`plugins/image-layouts`, order 55). **문법 계약만 공유하고 렌더러는 각자**
— 원고는 어느 렌더러에도 종속되지 않고, Obsidian 플러그인을 빼도 웹은 계속 동작한다.

**설계의 전부는 "진짜 mdast `image` 노드를 만든다"는 한 줄이다.** 작업량의 대부분은
문법 변환이 아니라 경로 해석(중첩 슬러그·첨부폴더·`public/` 복사)인데, `type: "image"`로
내보내면 CrawlLinks(60)와 Assets 이미터가 위키링크 이미지와 똑같이 처리해준다.
원시 HTML로 `<img>`를 만들었다면 저 셋을 전부 직접 구현해야 했다. 그래서 order가
CrawlLinks보다 **앞서야** 한다는 제약이 생긴다. 중첩 노트에서 `../../attachments/…`로
해석되는 것까지 실측으로 확인했다.

**캡션은 이미지 아래로 고정했다.** 원 플러그인의 오버레이(사진 위 캡션)를 이식하려면
배경 명도를 통제할 수 없어 테마 밖 고정색 스크림이 필요해지고, 그러면 "색의 단일 소스"
규칙이 깨진다. 아래로 내리니 `--gray`로 이미 검증된 대비를 그대로 쓰게 됐고 예외가
사라졌다. 색 규칙이 형태를 결정한 사례다.

계획 문서(`image-layout-handoff.md`)를 원본 소스와 대조해 **7건을 정정했다.** ASCII
그리드가 공백 구분이라는 것(문서 예시 `AAB`는 1열 그리드가 된다), 영역 이름이 letter가
아니라 `image-{n}`이라는 것, 컨테이너를 `paragraph`로 만들면 잘못된 mdast라는 것 등.
**설계 전에 원고와 원본을 세어본다**는 이 저장소의 원칙이 또 값을 했다.

빌드 함정 둘도 기록해둔다.
- **`yaml` 패키지를 걷어냈다.** node 조건에서 CJS로 해석돼 `__require("process")`
  셰임이 남고 런타임에 플러그인이 통째로 죽었다(`reading-time` 사고와 같은 계열).
  필요한 문법이 `key: 스칼라`와 블록 스칼라뿐이라 직접 파싱한다.
- **`quartz.lock.json`이 로컬 플러그인 9종을 다른 저장소(`haejunhyun-v5-test`)의
  절대경로로 고정하고 있었다.** 이 저장소를 고쳐도 빌드는 남의 코드를 쓰고 있었고,
  `plugin install`은 락파일을 따르므로 심링크를 지워도 되살아났다. 경로를 교정했다.

## 2026-08-06 (2차) — 되돌린 것 둘, 확정한 것 하나

**콜아웃 "인쇄 조판"을 되돌렸다.** 아이콘을 걷어내고 위아래 괘선 + 대문자 제목으로
바꿨더니 형태가 낯설어져 읽기가 더 나빠졌다(사용자 피드백: "너무 못생겼다").
Obsidian 형태(아이콘 + 둥근 모서리 + 좌측 3px 바)로 복귀하고, **우리가 정의하는 건
색(5그룹)이지 형태가 아니라는 것**을 분명히 한다. 앞선 진단("Obsidian처럼 보이는
원인은 형태")은 사실로선 맞았지만, 그 형태가 이미 읽기에 최적화된 관습이라는 점을
값으로 치지 않았다.

**패널 라벨의 모노 대문자 + 축 색도 되돌렸다.** 넷을 서로 다른 색으로 나누니 라벨이
콘텐츠보다 시끄러워졌고 "Table of Contents"가 대문자로 뭉갰다. SPECS를 기준으로
다섯을 한 규칙(Hahmlet 700 / 1rem / --darkgray)으로 통일했다 — 같은 종류의 물건은
같은 조판이어야 한다.

**헤딩 색의 근거를 확정했다.** "왜 --dark냐, 팔레트에 블루가 4개나 있는데"라는 지적을
실측했다. 두 가지가 드러났다.
- **블루는 4개가 아니라 2색 × 2모드다.** True Midnight/Deeper Cut은 라이트,
  Stage Light/Rim Light는 다크의 같은 두 자리(secondary/tertiary)다. 한 테마에서
  쓸 수 있는 블루는 2개뿐이고 둘 다 링크와 hover에 이미 배정돼 있다.
- **`--dark`는 검정이 아니다.** `#141d28`은 H213 S50으로 True Midnight(H207)과 6°
  차이인 **블루 계열의 가장 깊은 잉크**다. 헤딩 3단계는 이미 전부 블루 계열이었다.

그 과정에서 진짜 문제를 찾았다 — **h3(`#323e4c`)가 본문 `p`와 완전히 같은 색**이라
색으로는 구분되지 않았다. h3를 `--dark`로 올렸다. 블루로 갈 수 없는 이유도 실측으로
확정됐다: 라이트 모드의 더 깊은 블루(Deeper Cut 7.55)가 h2의 True Midnight(6.17)보다
**강해서 위계가 뒤집힌다.**

**AA 재검증에서 warn도 걸렸다.** 틴트가 5%에서 7%로 돌아오면서 `--cal-warn` `#8f6410`이
4.42로 미달 — `#8a600f`(4.69)로 교체. 이번 세션에서 틴트 배경 기준 재검산으로만
AA 미달 2건(good·warn)을 잡았다.

## 2026-08-06 — 팔레트를 본문으로: 헤딩·강조·패널 라벨 + 콜아웃 형태 개편

**"콜아웃이 여전히 Obsidian 같다"의 원인은 색이 아니라 형태였다.** CSS는 정상
적용돼 있었다(레이어 밖 확인, component CSS 34개 중 콜아웃 규칙 0건, 원고 문법
286개 = 렌더 286개). 그런데도 Obsidian으로 읽힌 건 **Lucide 아이콘 12종 + 둥근
모서리 + 좌측 3px 바**가 그대로였기 때문이다. 그 셋이 Obsidian 콜아웃의 형태
문법이라, 색을 아무리 정교하게 맞춰도 앱 UI로 보인다. 아이콘을 걷어내고 위아래
괘선 + 대문자 레터스페이스 제목으로 바꿨다. 제목은 사용자가 쓴 텍스트라
`text-transform`이 라틴에만 걸리고 한글 제목은 그대로 읽힌다.

**설계가 원고를 앞질렀던 것 — reference/quote.** `[!quote]` 70개의 제목을 세어
보니 "Reference" 50 · "참고사이트" 17로 **69개(98.6%)가 출처**였고 내용도 링크
목록이었다. 진짜 인용문은 0. 반대로 우리가 정의한 `[!reference]` 보라는 원고에서
0회 — 죽은 토큰이었다. DESIGN-SYSTEM이 "quote(누가 한 말)와 reference(어디서
왔나)는 다르다"고 나눠놨지만 실제 글쓰기에 그 구분이 없었다. vault의 67건을
`[!reference]`로 치환해 설계와 용법을 맞췄다(데모용 2건은 보존).

**AA 미달 1건 발견 — 콜아웃 배경 기준으로 재니 드러났다.** `--cal-good`
`#1a7f5a`는 페이지 배경에선 4.56이지만 **자기 틴트 배경 위에선 4.28**이었다.
CLAUDE.md §7("대비는 코드블럭 배경 기준으로 잰다")이 콜아웃엔 적용되지 않고
있었다. 색상을 유지한 채 명도만 낮춰 `#187755`(4.74)로 교체.

**헤딩이 Hahmlet으로 돌아오면서 bold의 색이 풀렸다.** 직전 세션에서 bold의 색을
뺀 이유는 헤딩과 `--dark`로 완전히 같아서였는데, 헤딩이 Hahmlet이 되면 **서체가
이미 둘을 가르므로** 그 제약이 사라진다. bold를 `--dark`로 되돌리고 h2에는
액센트 블루를 줬다 — 링크는 항상 인라인, 헤딩은 항상 블록 단독이라 같은 흐름에서
경쟁하지 않는다. `em`은 `--cal-quote`로 인용/출처 계열과 잉크를 공유한다.

**패널 라벨 4종을 모노 대문자로 통일하고 축 색을 부여했다.** CATALOG·BACKLINKS는
공간(블루), TABLE OF CONTENTS는 시간(Chitlins) — 헤더와 활성 항목이 같은 잉크라
패널 정체성이 강해진다.

**메타를 SPECS 블록으로 분리.** 제목 바로 밑에 뭉쳐 있던 `날짜, N min read`가
제목에서 본문으로 가는 길을 막고 있었다. 우측 최상단 음반 사양 표기로 옮기고,
모바일은 기존 content-meta/tag-list를 `display: mobile-only`로 남겼다.

⚠️ 두 함정: ① `reading-time` 패키지를 의존성으로 넣으면 CJS라 ESM 번들에서
`Dynamic require of "stream"`으로 **컴포넌트 로드가 통째로 실패**한다(에러가 아니라
경고 한 줄로 지나가서 "렌더는 되는데 안 보임"으로 나타난다) — 알고리즘을 이식했다.
② `defaultDateType: published`는 `published:` 프론트매터를 가진 노트가 0개라 항상
filesystem mtime으로 폴백했다. 수정한 적 없는 노트가 빌드 날짜로 찍히고 있었다 →
`modified`(89개 노트가 `updated:` 보유)로 교체.

## 2026-08-05 — 노트 상세 3패널 개편: 버그 4건이 먼저였다

"패널이 밋밋하다"는 지적을 추적하니 **설계 문제가 아니라 버그 4건**이 먼저 있었다.
빌드본을 로컬 서버로 띄워 computed style을 실측한 결과다.

**① 콜아웃의 3면이 팔레트 밖 색이었다.** `custom.scss`가 그룹별로 `--color`만
재정의하고 `--border`는 두어서, base `callouts.scss`의
`border: 1px solid var(--border)`가 살아 있었다. 좌측 3px만 우리 색이고
나머지 3면은 Obsidian 하드코딩 색 — note는 `#448aff44` 콘플라워, warning은
`#db894244` 오렌지, quote만 중립 `--lightgray`. 타입마다 규칙이 달라
"콜아웃 정의가 안 먹는다"로 보였다. §0 원칙("색의 단일 소스")이 콜아웃에서
깨져 있었던 것. 네 변을 전부 `--color`에서 파생시켜 신규 타입도 따라오게 했다.

**② KaTeX 크기는 설정 실수가 아니라 이중 보정이었다.** `custom.scss`의
`.katex { font-size: 1.05em }`은 **죽은 코드**였다 — `latex` 플러그인이
`externalResources()`로 주입하는 CDN CSS가 `index.css` 뒤에 링크되고,
KaTeX의 `.katex{font-size:1.21em}`와 명시도가 같아(0,1,0) 소스 순서로 CDN이
이긴다. 그리고 그 1.21은 KaTeX_Main(x-height/em ≈ 0.43)이 **세리프 본문** 옆에서
작아 보이는 걸 보정하는 값인데, 우리 본문은 Noto Sans KR(≈ 0.53)이라 보정이
필요 없는 자리였다. CDN 순서는 못 바꾸므로(`.quartz/`는 생성 디렉터리) `body`를
붙여 명시도로 이기고, 인라인은 `1.21 × (0.43/0.53) ≈ 1.0em`으로 되돌렸다.
"크기를 줄인" 게 아니라 **중복 보정을 제거**한 것이다.

**③ explorer `useSavedState: false`는 no-op이었다.** `Explorer.tsx:80`이
`data-savestate`를 내보내지만 `explorer.inline.ts`는 그 속성을 한 번도 읽지
않는다(`dataset` 접근은 `dataFns`/`behavior`/`folderpath` 셋뿐). 읽기(L247)도
쓰기(L338, L383)도 무조건이라 한 번 연 폴더가 localStorage에 영구 누적됐다.
`quartz.config.yaml`의 주석은 사실과 달랐다.

**④ TOC가 AA 위반이었다.** base가 비활성 항목에 거는 `opacity: .35`가
`--gray` 위에 곱해져 실효 대비 **약 1.6:1**. `custom.scss`는 `color`만 덮고
`opacity`는 두었다. 사용자가 지적하지 않은 항목인데 가장 심각했다.

**구조 변경 — explorer 폐기, CATALOG 신설.** ③을 우회로 고치는 대신 좌측 패널
자체를 바꿨다. 전체 트리(폴더 32·파일 89 전부 DOM 렌더)를 버리고 현재 노트의
형제 노트만 보여주면, 길이가 형제 수에만 비례하고 **펼침 상태가 존재하지 않아
누적 문제가 무관해진다.** 은유도 맞는다 — 상단 GlobalNav = 레이블 카탈로그,
좌측 = 지금 이 판의 트랙 리스트, 우측 = 이 트랙 안의 구조. 구 트리는
`/topics`와 기능이 겹쳤다. 라벨 "Explorer"는 IDE 용어라 **CATALOG**로.

**우측 3패널 높이 배분.** 셋 다 `flex: 0 1 auto`라 TOC가 자연 높이를 다 먹고
861px 사이드바에서 백링크가 **52px(한 줄)** 이었다. base가 백링크에 건
`max-height: calc(100% - 2rem)`도 부모 높이가 auto라 무시됐다. 순서를
TOC → Backlinks → Graph로 바꾸고 TOC에 `flex: 1 1 0`, 백링크 `max-height: 9rem`,
graph는 기본 접힘. graph는 노드가 1~2개뿐인 노트가 많아 250px를 상시 점유할
값어치가 없었다.

**색 결정 — Chitlins를 TOC 활성 마커까지만 넓혔다.** TOC의 in-view는 "지금 내가
읽고 있는 위치"라 읽기 진행바와 같은 질문에 답한다. 하이라이트는 후보였으나
제외했다 — 세 축 어디에도 속하지 않는 신호라 네 번째 색을 만들면 축 체계가
무너진다. 인라인 코드와 같은 해법으로 **명도로만 구분하는 웜 뉴트럴 밑줄 밴드**로
갔다(형광 노랑 `#fde68a88`은 팔레트 어디에도 근거가 없었다).

**bold/italic.** `strong`이 헤딩과 같은 `--dark`라 "강조"와 "구조"가 같은 신호를
썼다 — 색을 빼고 무게만 남겼다. `em`은 Noto Sans KR에 진짜 이탤릭이 없어
브라우저가 합성 오블리크를 만들고 한글엔 이탤릭 관습도 없으므로 톤 다운으로 바꿨다.
⚠️ 두 규칙은 레이어 밖 + `article` 스코프여야 한다 — 레이어 안에선 base의
`p > strong { color: var(--dark) }`가 명시도로 이기고, 전역 요소 선택자를
레이어 밖에 두면 플러그인 UI가 깨진다.

## 2026-08-05 — 코드 하이라이팅 재설계 ②: "종이 위의 잉크"

GitHub Light를 구조 참조로 삼은 게 **방향 오류**였다. 그건 순백(`#ffffff`)
배경용이고 우리 배경은 웜 크림(`#efebe0`)이다. 우리 배경 위에서 유명
테마들을 실측하니 전부 AA를 대량 미달했다 — Solarized Light 9/9,
Rosé Pine Dawn 6/8, Gruvbox Light 4/9, GitHub Light 3/7. **색 값을
차용할 대상이 애초에 없었다.**

문제는 대비가 아니라 조화였다. 이전안은 채도가 과했고(S77~87을 S6인
종이 위에), `function`의 `#7d3fb0`(H273 보라)은 브랜드 어디에도 없는
고아 색상이었으며, keyword(H11)와 alert(H350)가 21° 차라 둘 다 빨강으로
읽혔다.

Gruvbox Light가 웜 배경에서 호평받는 진짜 이유를 채도가 아니라
**어둡기(V≈60)**로 파악하고 그 구조만 가져왔다 — 화면 색이 아니라
인쇄 잉크로 읽히게. 유채색을 전부 눌렀다(function V69→49, value V58→38).

결과적으로 유채색 4종이 **전부 브랜드 색상에 1~5° 이내로 결합**됐다:
string=H206(공간·블루), keyword=H16(행위·브릭), function=H323(시간·Chitlins),
value=H167(딥 틸, 유일한 신규 잉크). 고아 색상이 사라졌다.

## 2026-08-05 — Shiki 폴백 버그: 변수명이 통제 밖에 있었다

"코드가 배경에 묻힌다"는 지적을 추적해 렌더 출력 7,598 토큰을 실측한
결과, **1,058개(13.9%)에 `data-token-type`이 없었다** — `hook`, `conn_id`,
`df` 같은 **변수명 전부**. 반면 내가 "가장 진하게"로 설계한 `normal`
타입은 **37개**뿐인 사실상 죽은 타입이었다.

즉 `--syn-ink`(13.59)는 거의 렌더되지 않았고, 실제 식별자는 폴백인
catppuccin-latte `#4C4F69`(6.70)로 떨어져 punct(5.88)·alert(6.64) 사이에
끼어 있었다. **내가 근거로 든 "스프레드 9.47"은 렌더되지 않는 값에 기댄
계산이었다.** 기본색을 `--syn-ink`로 바꿔 폴백을 없앴다.

같은 지적의 다른 절반은 목업 결함이었다 — `haejun-redesign-palette.html`이
`    hook = `을 통째로 `punctuation`으로 마크업하고 있었다. 실제 Shiki
출력에 맞게 수정.

## 2026-08-05 — 주석 압축 (config + 플러그인 소스 20파일)

주석 428줄 → 252줄(-41%). 날짜·세션 히스토리·목업 라인 참조·자기 정정
기록을 걷어내고 핵심 기능과 함정만 개조식으로 남겼다. **경고성 내용은
전부 보존**하고 `⚠️` 마커를 붙였다(예: "통계 하드코딩 금지", "플러그인에서
색 값 정의 금지", "position: header는 조용히 버려짐").

위의 "근거는 코드 주석으로 이관"(아래 항목)과 상충하지 않는다 — 이관된
것 중 **규칙**은 남기고 **서사**만 여기 CHANGELOG로 옮겼다.

정리 중 **틀린 주석 1건 발견**: `visitor-counter`가 "footer가 렌더한다"고
적혀 있었으나 실제로는 home-hero가 렌더한다.

## 2026-08-05 — 선행 문서 5종 삭제, 근거는 코드 주석으로 이관

`handoff.md` / `REDESIGN-GUIDE.md` / `design-handoff.md` /
`MIGRATION-NOTES.md` / `CODE_OF_CONDUCT.md` 삭제.

앞의 넷은 플러그인 소스 21곳에서 섹션 번호까지 찍어 참조되고 있었다
(`design-handoff.md` 12곳, `REDESIGN-GUIDE.md` 5곳, `MIGRATION-NOTES.md`
4곳). 그 주석들은 단순 출처 표기가 아니라 "무엇이 틀렸었는지"를 담고
있어서 — 예: "목업의 129 노트는 실측 90으로 틀렸음", "토큰 병렬 정의
금지" — 문서만 지우면 경고의 근거가 사라진다. **참조를 걷어내는 대신
각 문서가 갖고 있던 사실을 주석 안으로 옮겨 자체 완결되게 고쳤다.**
코드 변경은 없고 주석만 바뀌었다.

`CODE_OF_CONDUCT.md`는 fork로 딸려온 Quartz 커뮤니티의 문서로, 이
저장소(개인 블로그, 외부 기여 없음)와 무관해 삭제. `LICENSE.txt`는
fork 라이선스 의무이므로 유지.

## 2026-08-05 — 컬러 "True Midnight" 적용 (D안)

레퍼런스 커버 4장에서 픽셀 추출 → A/C/D 3안 비교 후 D안 확정.
라이트=크림 인쇄지(`#f7f5ef`), 다크=잉크 네이비(`#0e1420`), accent는
True Blue(H208)와 Midnight Blue(H202)의 중간값 `#1c5f95`(H207).

웜 액센트는 2종을 역할로 분리 — Chitlins(`#b03a7d`)=행동/CTA,
Brick(`#b8442a`)=기술 리터럴/인라인 코드. 두 색은 색상환 45° 간격.

작업 중 **AA 미달 2건을 발견해 함께 수정**: `--gray`(2.72:1)와
`--tertiary`(2.90:1). 후자는 링크 hover 텍스트 색이라 장식이 아니라
가독성 문제였고, hover 방향을 "밝아짐 → 진해짐"으로 바꿔 해결했다.

또한 `custom.scss`의 `rgba(61,107,142,·)` 하드코딩 15곳을 발견 —
"색의 단일 소스는 config"라는 원칙이 실제로는 깨져 있었다.
`--accent-rgb` 토큰으로 전부 치환.

기각: 다크모드 Hero 뒤 radial-gradient 광원(시안까지 만들었으나 제외).
보류: 토픽 컬러 10슬롯 AA 재계산, 코드블록 테마 웜 계열 교체.

## 2026-08-04 — Blue Note Bar/Blue Giant 컬러 비교 (§5.0)

도쿄 Blue Note Bar(네이비+베이지, 블루 네온 vs 브릭/가죽 온도 대비)와
Blue Giant(잉크 네이비→일렉트릭 블루 고채도 그라디언트) 팔레트를 현재
사이트 팔레트와 비교. 둘 다 몰입 환경용 색이라 본문에 그대로 쓰면
가독성·AA 대비가 깨져 전면 채택은 기각. 다크모드 배경/광원(Blue Note
Bar)과 인터랙션 순간(Blue Giant)에만 선택 반영하는 방향으로 §5 갱신.
아직 코드 미반영 — 방향 문서화만 완료.

## 2026-08-04 — 폰트 재결정: Gothic A1 → Hahmlet + Anton

Gothic A1(헤딩)+IBM Plex Sans KR(본문) 조합이 실물 확인 결과 가독성
저하로 판정 — 헤딩·본문이 같은 산세리프 계열이 되며 위계가 사라짐.
한글/라틴 이중언어 서체 Hahmlet(헤딩)으로 교체, 본문은 Noto Sans KR
유지. 로고는 Anton(브랜드 존 고립)으로 확정.

## 2026-08-04 — pageTitle: HYUNATLAS → HAEJUN RECORDS

GIS 메타포(Atlas)를 버리고 '기록'과 '인디 음반사'의 중의적 네이밍으로.
브랜드 컨셉을 Blue Note Records 무드로 전환.

## 2026-08-02 — 폰트 렌더링 버그 발견 및 수정

`fonts` 플러그인을 옵션 없이 켜두면 자기 기본값(Schibsted Grotesk 등)
으로 `configuration.theme.typography`를 덮어쓴다는 사실 확인 — 그동안
사이트는 의도한 명조가 아니라 기본 산세리프로 렌더되고 있었다. 옵션
명시로 해결.

## 2026-08-02 — design-handoff.md 검증 (REDESIGN-GUIDE.md 원본)

design-handoff.md가 가정한 아키텍처(Quartz v4)와 실제(v5 플러그인
체계)가 달라 F1~F7 교정 필요. 콘텐츠 규모도 목업(129)과 실측(90)이
어긋나 TopicGrid/Hero 카피 전면 재작성. 액센트 컬러는 핸드오프 제안
테라코타 대신 기존 슬레이트 블루 유지(AA 대비 우위).

## 2026-07-29 — handoff.md 원본 작성

폰트 변수 미적용, 콜아웃 색상 미분화, 인라인 코드 다크모드 대비 부족
등 P0~P2 12개 개선 항목 진단.

---

## 삭제된 문서

전부 2026-08-05에 삭제됨. 내용이 어디로 갔는지만 남긴다.

| 문서 | 내용의 행선지 |
|---|---|
| `handoff.md` | 진단 결과는 이 문서 타임라인, 현재 상태는 `DESIGN-SYSTEM.md` |
| `REDESIGN-GUIDE.md` | F1~F7 교정 사항은 해당 플러그인 소스 주석 |
| `design-handoff.md` | §3.1/§4.1/§4.2 스펙은 각 플러그인 소스 주석 |
| `MIGRATION-NOTES.md` | v5 API 함정은 이를 참조하던 4개 소스 주석 |
| `design-bluenote-handoff.md` | Gothic A1/IBM Plex Sans KR 제안 기각(2026-08-04 항목). 이미 삭제돼 있었음 |
| `CODE_OF_CONDUCT.md` | fork 잔재, 이 저장소와 무관 — 대체 없이 삭제 |

`docs/superpowers/plans/2026-07-18-quartz-v5-migration.md`에 남은
`MIGRATION-NOTES.md` 언급은 완료된 계획서의 기록이고 외부 경로
(`~/Developer/haejunhyun-v5-test/`)를 가리키므로 그대로 둔다.
